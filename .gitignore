node_modules
node_modules/
dist/
dist-engine/
.DS_Store
*.local
.netlify/

# Secrets / env
.env
.env.*
!.env.example

# CPython bytecode from scripts/mdlib-sanity.py — the snapshot ships verbatim
__pycache__/

# BodyParts3D downloads for scripts/build-anatomy-atlas.mjs — tens of MB, refetched on demand
.cache/

# SDD/eval scratch (ledger docs carry the numbers; the specs stay on disk, not in git)
.superpowers/
