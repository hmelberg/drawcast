# Editor shell — execution ledger and decision record

*2026-08-31. The controller's ledger from executing
`docs/superpowers/plans/2026-08-31-editor-shell.md` across 12 tasks and 31 commits.
Kept because the 45 rulings below are decisions taken on Hans's behalf during the run:
each says what was decided, why, and what it costs if wrong. The git history records
what changed; this records why it was allowed to change that way.*


## Pre-flight probes (run, not assumed)

| Probe | Result |
|---|---|
| `src/ui/controls.ts` imports in vitest `environment: "node"` | PASS — T1's test is viable |
| `parsePlaylistText('{"elements":[],"commands":[…]}')` → 1 item, commands intact | PASS — T7's test is viable |
| `src/store.ts` imports without a DOM | PASS — T9's test is viable |
| `h("div", {})` in node | THROWS `document is not defined` — confirms every test must import only pure functions, never a DOM builder. All 6 planned test files comply. |

## Pre-flight conflict scan

### Pairs sharing a file or interface

| Pair | Produces → consumes | Finding |
|---|---|---|
| T1 → T12 | both edit `src/ui/controls.ts` | Different regions (750–771 idle vs 717/730–743 bar). Clean. |
| T2 → T3,T5,T8,T12 | `--bar-h`, `--tap` tokens | Clean. |
| T2 self | absorbs the old `@media (hover: none)` at styles.css:565 | **DEFECT** — if the implementer adds a block without deleting the old one, T2's regex matches only the first and the test passes on the wrong block. |
| T2 → T3 | touch tier lists `.menu-item`, created two tasks later | Harmless forward reference; CSS for an absent class is inert. |
| T3 → T7,T8,T12 | `createMenu`, `visibleItems`, `MenuItem` | Names consistent across all three consumers. Clean. |
| T4 → T5,T6,T7,T9,T10 | `Modal.footer`, `size`, `.modal-s/m/l` | Clean. |
| T4 → T10 | T10 needs `ModalOptions.head`, which T4 does not add | **DEFECT** — `createModal` would be rewritten twice. |
| T4 → T10 | T4 sets course modal `size:"l"`; T10 edits the same call | Sequential. Clean. |
| T5 → T6 | T5 converts `ytDialog`, T6 dismantles it | Wasteful but deliberate: keeps the suite green between tasks. Plan says so. Clean. |
| T6 → T9,T10 | `ShareTo`, `shareBtn`, `subject:"course"` | **DEFECT** — T6's Interfaces block says `share.ts` produces `ShareTo`; T6 step 5 says `store.ts` declares it. Contradiction. |
| T6 → T8 | T8's bar references `shareBtn` | Ordering fine. Clean. |
| T7 → T8 | `insertMenu` | Ordering fine. Clean. |
| T9 → T10 | Backup moves out of course.ts in T9, asserted gone in T10 | Redundant but not conflicting. Clean. |
| T11 → none | `refreshLibrary` region is touched by no other task | Clean. |
| T12 → T2 | `max-width: 560px` vs `hover: none` | Different queries; T12's test uses matchAll over all 560px blocks. Clean. |

### Self-consistency, per task

| Task | Its tests vs its code | Finding |
|---|---|---|
| T1 | imports `shouldIdle` from controls.ts | Clean (probed). |
| T2 | CSS drift assertions | Defect above. |
| T3 | imports `visibleItems`; `document` touched only inside `createMenu` | Clean. |
| T4 | CSS + modal.ts | Clean. |
| T5 | `/h\(\s*"dialog"/` absence | Clean. |
| T6 | pure `shareDestinations` | Defect above. |
| T7 | playlist round-trip | Clean (probed). |
| T8 | icon-only absence | Clean. |
| T9 | `SETTINGS_TABS` | Clean (probed). |
| T10 | `/class:\s*"pane-bar"\s*\},([^)]*)\)/` | **DEFECT** — `[^)]*` truncates at the spacer's `)`. Verified against real source: captures 14 entries, passes only by accident of where it truncates. |
| T11 | pure `sidebarSections` | Clean. |
| T12 | CSS drift assertions | Brittle but verified workable. |

## Rulings (pre-flight)

Ruling: `ShareTo` is declared and exported from `src/store.ts`, and `src/ui/share.ts` imports it — not the reverse. Why: `store.ts` is imported by the standalone viewer chunk and must not pull in UI code; the plan's own step 5 says this and its Interfaces block contradicts it. Cost if wrong: a type lives in a slightly odd place; a one-line move.

Ruling: `ModalOptions` gains `head?: HTMLElement[]` in Task 4, not Task 10. Why: `createModal` is written once instead of twice, and T10 then only uses it. Cost if wrong: Task 4's diff is three lines larger than the plan says.

Ruling: Task 2 must assert there is exactly ONE `@media (hover: none)` block in styles.css. Why: the plan tells the implementer to absorb the existing rule at :565, and a regex over the first match cannot tell absorption from duplication. Cost if wrong: none — the assertion is strictly stronger.

Ruling: Task 10's twelve-controls assertion is replaced by explicit absence checks (`publishBtn`, `bakeLabel`, `backupBtn`, `saveBtn`, `courseSel` absent from the `pane-bar` call). Why: the planned regex passes by accident of truncation, verified against the real file; a test that can pass for the wrong reason is not a test. Cost if wrong: none — it asserts the same intent more directly.

Ruling: work proceeds on `main` rather than a worktree. Why: this repo's convention is push-to-main, and Hans approved a plan whose every task ends in `git push origin main`. Cost if wrong: a revert of a pushed range rather than an abandoned branch.

## Progress

Task 1: dispatched (sonnet, general-purpose). BASE=a44083b. Brief task-1-brief.md, report task-1-report.md.
Ruling: Task 1 Step 1 (reproduce on a physical phone) is skipped by the implementer and handed to Hans — a subagent cannot hold a phone. `shouldIdle` is implemented regardless, as the brief itself directs. Cost if wrong: a guard that was only ever a precaution.
Ruling: Task 1 Step 6 wires gate-presence by `stage.querySelector` for the `cs-waitgate`/`cs-cardgate`/`cs-figgate` overlay classes rather than threading a `gateOpen` flag through six module-level gate factories. Why: the factories are defined outside the scope that owns the idle timer, so a closure assignment is impossible without restructuring all six; the overlays are already in the DOM at exactly the moment the question is asked. Cost if wrong: if a future gate ships an overlay without a `cs-*gate` class, the bar could idle out during it — mitigated by the test pinning the invariant.
Task 1: implementer DONE (commit 0532345, 148 files / 2528 tests green, tsc clean). Used the EXISTING `gateIsOpen(stage)` in src/ui/gates.ts rather than a hand-rolled querySelector — better than my ruling: zero edits to the six gate factories, and it satisfies tests/gates.test.ts's drift guard against hand-written gate-class lists.
Task 1: implementer self-flagged concern — the schedule-time check does not cancel an already-armed timer nor force-reveal an already-faded bar the instant a gate opens; it relies on pointerdown bubbling. Sent to the reviewer to adjudicate rather than deciding it myself.
Task 1: review dispatched (sonnet). Package review-a44083b..0532345.diff.
Task 1: review — spec ✅, quality Approved with 2 Important findings. (a) the schedule-time-only check is a real, reachable gap: a timer armed before a gate opens still fires and hides the bar during the question, contradicting the code comment's own guarantee; (b) the implementer's report claim that all six gates self-heal via bubbling is WRONG for `pianoGateFor` (controls.ts:284-285) — it listens on pointerdown and calls stopPropagation, so the bar can stay hidden through and past a piano question. Reviewer verified all six gate→class mappings and ran idle-policy+gates tests itself (9/9).
Ruling: the MutationObserver the implementer set aside as scope creep is IN scope for Task 1. Why: the task exists to guarantee the controls stay reachable during a question, and a guarantee that holds only for the five gates which happen not to call stopPropagation is a coincidence, not a guarantee. Cost if wrong: ~10 lines of observer wiring in controls.ts that a later reader may judge unnecessary.
Task 1: fix round 1/5 dispatched (resumed original implementer). FIX_BASE=0532345.
Task 1: fix round 1/5 implemented (commit f3fef1a, 2528 tests green, tsc clean). Fire-time re-check + MutationObserver on stage childList. Implementer flagged: no observer.disconnect(), relying on per-render node disposal — sent to the re-reviewer to verify rather than accepting.
Task 1: scoped re-review dispatched (sonnet). Package review-0532345..f3fef1a.diff.
Task 1: re-review — BOTH findings ADDRESSED, new breakage none. Fire-time check + observer each independently cover the scenario; observer fires on DOM mutation so pianoGateFor's stopPropagation is irrelevant to it. Leak claim VERIFIED (render/index.ts:92-95 makes a fresh figure/stage per render; all three attachPlayerControls call sites in playlist/session.ts pair 1:1). svg-backend claim VERIFIED (mount appends once at :797; swapGeometry/remount only replaceChildren inside the persistent svg).
Task 1: complete (commits a44083b..f3fef1a, review clean). PUSHED.
Task 2: dispatched (sonnet). BASE=f3fef1a. Brief task-2-brief.md, report task-2-report.md.
Task 2: implementer DONE (commit 72ce96a, 149 files / 2533 tests green) and correctly flagged a PLAN defect rather than silently deviating: `.cs-progress` has `overflow: hidden` (styles.css:719), so the brief's `::before { inset: -10px 0 }` touch hit area is clipped to nothing. Verified by me.
Ruling: the seek bar's touch hit area comes from content-box vertical padding on `.cs-progress` itself (box-sizing:content-box; height 12px; padding 14px 0; background-clip:content-box; overflow:visible; fill gets its own border-radius), not a pseudo-element. Why: overflow:hidden exists to round the fill and cannot be kept while a ::before escapes the box; I checked the seek handler (controls.ts:943-948) and it maps the X axis only, so vertical padding cannot disturb seeking. Cost if wrong: the visible track's rounded ends render slightly differently on touch; a two-line CSS revert.
Ruling: the shell-css assertion for the seek bar must check the padding as well as the height. Why: the height-only assertion passed against the clipped, non-working version — that is how the defect reached the implementer at all. Cost if wrong: none, strictly stronger.
Task 2: fix round 1/5 dispatched (resumed implementer). FIX_BASE=72ce96a.
Task 2: fix round 1/5 implemented (commit cf19f73, 2533 tests green). Test now asserts height AND padding.
Task 2: review dispatched (sonnet) over the full task range f3fef1a..cf19f73 — the fix landed pre-review, so this is the task review, not a scoped re-review.
Task 2: review — spec ✅, quality Approved. Important: the seek-bar test asserts height+padding but NOT `box-sizing: content-box`; drop that one declaration and the global `* { box-sizing: border-box }` (styles.css:23) makes 12px the TOTAL height, padding collapses the content box, tap target silently returns to ~12px with the suite green. Minor: 12+14+14 = 40px undershoots the `--tap: 44px` token two lines away. Reviewer independently verified (a) the content-box override does outrank the universal selector, (b) `.cs-progress-fill` height:100% resolves against the 12px content box so `overflow: visible` cannot let it escape, (c) padding is vertical-only so no horizontal overflow, (d) `--bar-h` is applied at styles.css:432 not merely defined.
Ruling: the Minor is folded into this fix round rather than deferred — padding becomes 16px 0 so the rule hits `--tap` exactly. Why: the file is already open, it costs one character, and a rule that misses the token sitting two lines above it invites the next reader to change the token instead. Cost if wrong: 4px more vertical space in the player bar on touch.
Ruling: the implementer must watch the new assertion FAIL (delete box-sizing, run, restore) before committing. Why: twice on this task an assertion would have passed against a broken implementation; an assertion nobody has seen fail is not yet a test. Cost if wrong: one extra test run.
Task 2: fix round 2/5 dispatched. FIX_BASE=cf19f73.
Task 2: fix round 2/5 implemented (commit febc24b, 2533 tests green). Implementer reports it watched the box-sizing assertion fail then pass, per the ruling.
Task 2: scoped re-review dispatched (haiku — small fix diff). Package review-cf19f73..febc24b.diff.
Task 2: re-review — both findings ADDRESSED, new breakage none. Falsifiability evidence present and real: failing run "1 failed / 4 passed" with the error on the box-sizing assertion, passing run "5 passed (5)". Arithmetic 12+16+16=44 confirmed against --tap.
Task 2: complete (commits f3fef1a..febc24b, review clean). PUSHED.
Task 3: dispatched (haiku — the brief carries complete code for menu.ts, its CSS and its test; transcription plus testing). BASE=febc24b.
Task 3: implementer DONE (commit c47f06d, 2536 tests green, no concerns raised).
Task 3: review dispatched (sonnet). Package review-febc24b..c47f06d.diff. I asked it to audit FOUR things in my own supplied code, since this primitive has four consumers and a defect propagates: the never-removed document click listener, the open-gesture/dismiss event ordering, the single-item collapse's button copy (`Open From disk…` is the common path for a user without Drive), and what createMenu renders when visibleItems is empty.
Task 3: review — spec ✅ (faithful transcription), quality NOT APPROVED. All four defects are in MY supplied code, not the implementer's work. CRITICAL: the trigger's `e.stopPropagation()` stops the click reaching document, so no other menu's close listener ever fires — two panels can stand open at once in Task 8's bar group, and the code comment claims the opposite. Important: document listeners never removed, and attachPlayerControls runs per playlist navigation (session.ts:204,322,417), so Task 12's overflow menu leaks one stale listener per jump. Important: empty visibleItems falls through and renders a live trigger over an empty panel — the trigger is itself an unadvertised-capability violation. Minor: "Open From disk…" mid-sentence capital on the default no-Drive path.
Ruling: replace stopPropagation with a containment guard (`root.contains(e.target)`) plus attach-on-open/remove-on-close. Why: one change fixes the Critical and the leak together — a listener that exists only while its menu is open cannot accumulate, and the guard is what lets a sibling menu's click close this one. Cost if wrong: dismissal misbehaves in a way only a live browser shows, since node cannot test it.
Ruling: the Minor is folded in — those lines are being rewritten regardless, and the collapsed label becomes "Open from disk…" by lowercasing the item's first character. Cost if wrong: one string reads oddly.
Ruling: the collapsed-label composition is extracted as an exported pure `soloLabel(verb, item)` and unit-tested. Why: the reviewer correctly noted the three existing tests cover none of these defects because createMenu needs a DOM; the label composition is the one piece node CAN test, so it should not stay untestable inside createMenu. Cost if wrong: one extra tiny exported function.
Task 3: fix round 1/5 dispatched. FIX_BASE=c47f06d.
Task 3: fix round 1/5 implemented (commit d95eb7b, 2541 tests green; menu-model now 8 tests — 3 visibleItems + 5 soloLabel).
Task 3: scoped re-review dispatched (sonnet — event-ordering logic warrants tracing, not summarising). Package review-c47f06d..d95eb7b.diff. Asked specifically whether B's onDocClick, added to document DURING the same click's propagation, fires for that very event and whether the containment guard saves it — the one way this fix could be subtly wrong.
Task 3: re-review — all FOUR findings ADDRESSED. Reviewer traced the race properly and confirmed the correct outcome: B's onDocClick IS invoked for the opening click (listener lists are fetched when dispatch reaches the node), document listeners run in registration order so A closes first, then B's containment guard suppresses its own close. Item clicks close synchronously in the target phase before document is reached, so no double-fire. `root` is a const referenced only inside later-invoked closures — no TDZ.
Task 3: parked — reviewer's NEW Minor ("`class=menu` + `hidden` does not truly collapse the box because `.menu{display:inline-flex}` is author-origin and outranks the UA `[hidden]` rule") — Ruling: the finding is WRONG and the code stands. The project already carries `[hidden] { display: none !important; }` at styles.css:31, with a comment naming this exact hazard; author `!important` beats author-normal, so the empty menu does collapse. The reviewer weighed only the UA rule and missed the project's own override. Cost if wrong: an invisible zero-size span in a toolbar, which is what the reviewer itself said the practical impact would be.
Task 3: complete (commits febc24b..d95eb7b, review clean, 1 parked). PUSHED.
Task 4: dispatched (sonnet — multi-file: modal.ts, styles.css, 5 main.ts call sites, course.ts, 2 test files). BASE=d95eb7b.
Task 4: implementer DONE (commit c3bc016, 2543 tests green, tsc clean) and raised TWO correct concerns, both defects in my brief. (a) Step 3 deleted the blanket `.dialog-body { overflow-y: auto }` and the replacement sets only max-height per size — a cap that does not scroll, in every modal, and it would have made Task 5's stated purpose ("the scroll cap every other dialog already had") a cap that capped nothing. (b) Moving the Instructions prompt buttons into the shared footer put them on the References tab too, since the footer is a sibling of the tab strip.
Ruling: restore `overflow-y: auto` as a base `.dialog-body` rule with the size classes varying max-height only, and ADD a test asserting it. Why: the property carrying the behaviour must not live only in the size rules; and this reached the implementer precisely because nothing tested for it. Cost if wrong: none.
Ruling: revert the Instructions footer move — those buttons stay in their tab panel. Why: the spec's own rule exempts "verbs that act on something inside the body" from the footer, and Save/Rename/Copy/Delete act on the prompt selected in the list inside the body, exactly like the per-template Delete the spec names. My Step 5 instruction contradicted the spec it was implementing; the tab-bleed was the symptom, not the disease. Cost if wrong: the Instructions modal keeps an empty footer, which :empty hides.
Task 4: fix round 1/5 dispatched. FIX_BASE=c3bc016.
Task 4: fix round 1/5 implemented (commit ca94a49, 2544 tests green, tsc clean). Both findings fixed as directed.
Task 4: review dispatched (sonnet) over the full range d95eb7b..ca94a49. Flagged one risk for it to determine: the brief dropped `max-width` from the bare `dialog` rule, but FOUR dialogs are still hand-built with no size class until Task 5 (Settings, author, 3D, YouTube). With `dialog { width: 92vw }` still in place they may now render far wider than before — and since this repo pushes after every task, an intermediate regression would reach users rather than being invisible mid-branch.
Task 4: review — spec ✅, quality NOT APPROVED. CRITICAL confirmed: the bare `dialog` rule lost `max-width: 30rem` while keeping `width: 92vw`, so Settings (main.ts:1106, no class) and the YouTube upload (main.ts:3335, `.yt-dialog` styles only `.yt-field`) go from a 480px cap to ~92vw — about 3x wider, shipping on the next push. The 3D and author dialogs set their own `width: min(...px, 92vw)` and are unaffected. Important: no test covers un-classed dialogs, which is why a fully green suite shipped it.
Ruling: restore `max-width: 30rem` on the BASE `dialog` rule rather than adding `modal-s` to the two call sites. Why: Task 5 converts both imminently, and the durable lesson is that the base element must be safe when a size class is forgotten — a modal that names no size should come out small, not full-bleed. Cost if wrong: `.modal-m`/`.modal-l` must win on specificity, which I asked the implementer to verify explicitly for `.modal-l`'s width.
Ruling: add an assertion on the bare `dialog` rule and watch it fail before committing. Why: this is the third defect on this plan that a green suite did not see; the pattern is assertions that cover the new thing and not the thing it replaced. Cost if wrong: one more test run.
Task 4: fix round 2/5 dispatched. FIX_BASE=ca94a49.
Task 4: fix round 2/5 implemented (commit 3a987a7, 2545 tests green, tsc clean). Implementer reports it watched the new bare-dialog assertion fail then pass, and argues .modal-m/.modal-l (0,1,0) outrank the type selector `dialog` (0,0,1) so the course modal does not shrink.
Task 4: scoped re-review dispatched (haiku — small diff). Package review-ca94a49..3a987a7.diff. Asked it to check the new assertion actually targets the BARE dialog rule and would not also be satisfied by `.modal-s { max-width: 30rem }`.
Task 4: re-review — both findings ADDRESSED, new breakage none. Assertion confirmed to target the bare rule via /\ndialog\s*\{([^}]*)\}/ (cannot be satisfied by .modal-s); both failing (1 failed | 8 passed) and passing (9 passed) runs present with real output; specificity reasoning confirmed correct, .modal-l does not shrink.
Task 4: complete (commits d95eb7b..3a987a7, review clean). PUSHED.
Ruling: Task 5 keeps the plan's `backdropCloses: false` on the YouTube dialog even though the original defaulted to true. Why: modal.ts documents that flag for exactly this case — a dialog holding work in progress that a stray click would discard, here the translated playlists in `ytTranslations` and a running upload. It is a deliberate behaviour change, recorded rather than smuggled, and Task 6 dismantles this dialog into Share regardless. Cost if wrong: a user must use ✕ or ESC to dismiss the upload dialog for one task's duration.
Task 5: dispatched (sonnet — four hand-built dialogs converted in main.ts). BASE=3a987a7.
Task 5: implementer DONE_WITH_CONCERNS (commit 5885521, 2546 tests green, tsc clean). Two concerns, both sound.
Ruling: the 3D dialog keeps its real title "⬡ Explore in 3D"; my brief's "3D" was shorthand for identifying the dialog, not an instruction to rename. The implementer read the ambiguity correctly (every other brief title matched code exactly; only this one diverged with no note marking it intentional). No change. Cost if wrong: none.
Ruling: adopt the scale for `.author-dialog` — it stays size "m" and narrows 880px -> 736px, and its now-dead `width: min(880px, 92vw)` is DELETED. Why: the scale exists so no modal carries a bespoke width, and a fully-overridden rule lies to the next reader about how wide the dialog is. Adding a fourth size or a one-off to preserve 880px would defeat Task 4 entirely. HANS-VISIBLE: this narrows the template-authoring surface by 144px — surface it in the final summary as a change he may want reverted. Cost if wrong: one line to restore, or size "l".
Ruling: also audit `.yt-dialog`, `.model3d-dialog`, `.sub-dialog`, `.course-modal` for width rules their size class now overrides; delete only genuinely dead ones. `.model3d-dialog` at 640px is UNDER the 736px cap so its rule is still load-bearing and stays. Cost if wrong: a dead rule survives, or a live one is deleted and a dialog resizes.
Task 5: fix round 1/5 dispatched. FIX_BASE=5885521.
Task 5: fix round 1/5 implemented (commit bf5a622, 2546 tests green, tsc clean). Deleted .author-dialog's dead width; ALSO found and deleted .sub-dialog's `width: min(520px, 92vw)`, arguing modal-s's 480px cap always won so it never took effect. Kept .model3d-dialog (640px genuinely under the 736px cap) and .yt-dialog (targets a descendant .yt-field, not dialog width). .course-modal never had one. Regression guards added for both deletions.
Task 5: review dispatched (sonnet). Package review-3a987a7..bf5a622.diff. Told it to independently verify the .sub-dialog "always dead" argument — the implementer extended my ruling on its own initiative, and if that rule ever took effect its deletion is an unrequested visible change. Also told it to check the ACTUAL point of the task: whether Settings' content now sits inside .dialog-body, since appending to the dialog instead would mean the task achieved nothing.
Task 5: review — spec ✅, quality Approved, no Critical/Important. THE POINT IS ACHIEVED: Settings content confirmed inside settingsModal.body -> .dialog-body -> overflow-y:auto + .modal-m max-height (styles.css:810-811). All four dialogs converted (no `h("dialog"` left in main.ts or course.ts). All 6 .settings-note strings and the yt-warning are byte-identical (untouched context lines in the diff). showModal/close/addEventListener("close") call sites unchanged; no prior dialog had onClose.
Task 5: minor (deferred): authorRefineBtn moved to the footer though the brief named only authorGenBtn/authorSaveBtn — correct call (refine/save show and hide as a pair) and flagged in the report, not silent.
Task 5: minor (deferred): .sub-dialog width deletion was outside the brief, but the reviewer independently confirmed the rule was ALREADY dead at 3a987a7 (520px > .modal-s's 480px cap), so it changes no rendered pixel.
Task 5: complete (commits 3a987a7..bf5a622, review clean). PUSHED.
Task 6: dispatched (sonnet — largest task: new src/ui/share.ts, store.ts type+setting, main.ts bar surgery, absorbs ytDialog). BASE=bf5a622.
Task 6: implementer DONE_WITH_CONCERNS (commit 4794a5a, 151 files / 2552 tests green, tsc clean). +719/-361 across main.ts, store.ts, styles.css, new src/ui/share.ts (606 lines), tests/share-destinations.test.ts. Three disclosures: (a) NEAR-MISS CAUGHT — sourceLanguage()/playlistWithSpecs() looked YouTube-only but are also used by the CC-subtitles feature, so it restored copies in main.ts before deleting the YouTube section, otherwise subtitles would have broken; (b) the residual "with narration" grep hits are a pre-existing unrelated error string in publishTextFor's bake guard, not a duplicated control, and it declined to reword user-facing text just to satisfy a grep; (c) it had to make all DOM construction lazy inside openShare() because a module-scope h() crashes vitest's node environment — which would have broken the brief's own Step 1 test.
Task 6: review dispatched (OPUS — largest diff in the plan, and the translation trap is the one path that can corrupt a user's document). Package review-bf5a622..4794a5a.diff. Told it to judge, not assume, the three disclosed decisions — especially whether leaving copies of sourceLanguage/playlistWithSpecs in main.ts created logic that can now drift out of sync.
Task 6: review (opus) — spec ✅, quality NOT APPROVED. TRANSLATION TRAP CLEAN: every doc/spec path traced, playlistWithSpecs spreads into fresh objects, ytTranslations holds only those, ytSaveCopy writes new UUID rows, nothing writes to doc. yt-warning byte-identical; 19 language checkboxes, privacy, both burn checkboxes preserved; all call sites unchanged; the "with narration" claim verified (main.ts:3055 pre-existing, count 1 at bf5a622).
CRITICAL: share.ts:558-567 selectDestination iterates the FILTERED destinations list, so a filtered-out destination never gets panels[...].hidden set, and panels are built with no hidden attribute — a user with GitHub but no Google and no TTS sees the whole YouTube panel (19 language checkboxes, the upload warning) and the video panel alongside Link. That is the DEFAULT state for most users and the exact inverse of the credential rule.
Important: backdropCloses:false was dropped; it also guarded translations, so a stray backdrop click after translating three languages costs those Anthropic calls twice (prepPanels clears ytTranslations on next open).
Ruling: fix the panel iteration AND extract a pure `panelVisibility(all, available, selected)` with a unit test. Why: the reviewer's real point is that the existing tests cover only the filter, and this is exactly the logic a pure function would have caught. Cost if wrong: one small extra exported function.
Ruling: Share's spec-format select becomes READ-ONLY w.r.t. settings (defaults from settings.specFormat, never writes it). Why: it matches the ⬇ button it replaces, which only read the setting, and it removes the editor formatSel desync at the source instead of adding a sync call. Cost if wrong: the format choice does not persist between Share opens.
Ruling: Share's burnCaptions WRITE stays (it is the destination's own option and persisting is right); the stale end is fixed instead — openSettings() refreshes its checkboxes from settings on open, as it already does for usageNote. Cost if wrong: a checkbox refresh runs on every settings open.
Ruling: remove the sourceLanguage/playlistWithSpecs duplication properly by parameterising them as pure exported functions taking the playlist, rather than pinning two copies with a drift test. Why: pure and parameterised is testable in this node environment; two copies plus a fragile drift test is not. Cost if wrong: a slightly wider import surface.
Task 6: fix round 1/5 dispatched. FIX_BASE=4794a5a.
Task 6: fix round 1/5 implemented (commit 175e4ff, 151 files / 2560 tests green, tsc clean). All four addressed; implementer reports it showed the new panelVisibility test failing against the pre-fix commit before passing against the fix.
Task 6: scoped re-review dispatched (sonnet). Package review-4794a5a..175e4ff.diff. Told it the de-duplication is the riskiest part of this round: playlistWithSpecs moved into playlist/playlist.ts and now serves BOTH CC subtitles and YouTube, so it must still build fresh entry objects — exportSequence hands out the document's own spec objects, and a mutation here would reintroduce the very hazard the original review cleared.
Task 6: re-review — all FOUR findings ADDRESSED, new breakage none. panelVisibility is pure (share.ts:256-259); selectDestination loops ALL_DESTS (share.ts:454-455); refresh() runs on every open and falls back to destinations[0] when the remembered shareTo is not offered, so a filtered-out panel is hidden on open, not merely after a rail click. Reviewer INDEPENDENTLY checked out 4794a5a's share.ts against the new test file and reproduced the 4 failures — real evidence, not asserted. playlistWithSpecs still builds fresh entries via .map(), pinned by a test asserting the original spec is unchanged and output !== input. No import cycle.
Task 6: complete (commits bf5a622..175e4ff, review clean). PUSHED.
Task 7: dispatched (sonnet — new src/ui/insert.ts, portrait handlers, the window.prompt goes). BASE=175e4ff.
Pre-dispatch check on Task 8 found a STALE instruction in my plan (caught before dispatching, not after). The brief said `downloadSpec()` is "the body of the old exportBtn handler" and that "importBtn and exportBtn are deleted" — but Task 6 already absorbed `⬇ exportBtn` into Share's Spec file destination (share.ts specGo:234-256), so no such handler exists. `importBtn` does still exist (main.ts:554) and Task 8 correctly folds it into Open ▾.
Ruling: `Save ▾` holds Google Drive ALONE — no "To disk…" item. Why: Share's Spec file destination is already the built, tested download path; a second one in Save ▾ would put a single action in two menus, which is precisely what §1 forbids, and Share is the single answer to "how does this document leave the app". The resulting asymmetry with Open ▾ is honest: opening a file from disk has no other home, saving one does. With Drive unconfigured the menu's only item is hidden and createMenu renders nothing, matching the credential rule. Cost if wrong: a user who wants a local spec file must go through Share rather than Save — one extra concept to learn, reversible by adding one MenuItem.
Plan patched at docs/superpowers/plans/2026-08-31-editor-shell.md and task-8-brief.md regenerated (94 lines).
Task 7: implementer DONE_WITH_CONCERNS (commit c17392f, 152 files / 2566 tests green, tsc clean). Three disclosures: (a) my brief said the portrait tooltip was 222 chars; it measured 160 and moved the real text, dropping a "leave the prompt empty" clause it judged obsolete now there is no shared prompt field; (b) the briefed single-key `PortraitSource` union cannot carry the filename-derived caption+provenance the old file-upload path set alongside `strokes`, so it kept portraitInsert's specified signature and patched those two fields on from the UI layer instead — avoiding a silent regression but splitting element construction across a tested and an untested half; (c) my brief's line numbers were stale, it used actual locations.
Task 7: review dispatched (sonnet). Package review-175e4ff..c17392f.diff. Told it to judge (b) rather than accept it: whether the two construction sites can drift, whether the file-upload output is genuinely identical to before, and whether widening the type would have been better.
Task 7: review — spec ✅, quality Approved. Verified: portraitInsert pushes {draw:id} and plan.ts:186-191 puts that id in `mentioned`, excluding it from the implicit tail-draw at :515-519 — the task's whole point achieved. window.prompt for portraits gone (the survivor at :3317 renames a saved prompt, unrelated). Eager resolution preserved (insert.ts:527-537 probes before commit). traceFromBlob moved wholesale, import cleanly removed. Cameo/corner semantics correct. All 5 tests non-vacuous. insertMenu name matches Task 8. pinPortraitsBtn byte-for-byte untouched. Disclosure A verified (tooltip really was 160 chars; the dropped clause really is obsolete). Disclosure B judged reasonable and honestly flagged, but with a real coverage gap.
IMPORTANT (new, neither the implementer nor I had it): insert.ts:481-537 captures the playlist BEFORE the trace/resolve await, whereas the deleted handler read it INSIDE the .then() after the delay. A spec-text change during the delay is silently clobbered by the stale snapshot — and the deleted code's own comment (main.ts:2993-2994) named this exact hazard. Outside the sanctioned placement-only exception.
Ruling: read the playlist AFTER the await, matching the old handler, and carry its reasoning across as a comment. Cost if wrong: none; it restores prior behaviour.
Ruling: REVERSE MY OWN BRIEF — widen PortraitSource to carry caption/provenance alongside strokes, and move the patching into portraitInsert so the pure function builds the whole element. Why: the implementer was right to stay inside the signature I gave and flag the cost, but the signature is what is wrong; leaving a post-hoc patch in untested UI glue is a drift waiting to happen. Cost if wrong: a slightly wider union type.
Task 7: fix round 1/5 dispatched. FIX_BASE=c17392f.
Task 7: fix round 1/5 implemented (commit dd71c52, 152 files / 2567 tests green, tsc clean). readPlaylist moved into a fresh apply() closure called right before commit(), with a WHY comment quoting the deleted code's hazard warning; PortraitSource's strokes arm widened so portraitInsert builds the whole file-upload element and the post-hoc patch is gone. Unrequested addition disclosed: it now reports an error instead of falsely closing if the chosen part vanished during the wait.
Task 7: scoped re-review dispatched (sonnet). Package review-c17392f..dd71c52.diff. Told it to trace ALL THREE insert paths (name, URL, file) — a stale-snapshot fix applied to only one path is not a fix — and to judge whether the unrequested vanished-part error is sound or scope creep.
Task 7: re-review — both findings ADDRESSED, new breakage none. All THREE modes route through the fresh apply(source) closure (insert.ts:191-195); name/URL inside resolvePortraits().then() at :235, file inside traceFromBlob().then() at :208; the old top-of-handler read is gone. Only part/cameo/afterStep/mode are read early — dialog-local UI state, no staleness risk. PortraitSource widened (insert.ts:27), portraitInsert spreads choice.source at :59, commit's patch parameter removed entirely, no residual el.source=/el.of= anywhere. New combined-shape test non-vacuous; original five byte-for-byte unchanged. The unrequested "part vanished" error path judged SOUND and in scope — a direct corollary of reading fresh, replacing the old "el undefined -> still report success and close" behaviour. Nothing leaked.
Task 7: minor (deferred): current.applyPlaylist(result) is still called unconditionally on the vanished-part error branch (insert.ts:166), so the editor text reformats/rerenders though nothing was inserted. PRE-EXISTING — the call was already unconditional before the fix — so not new breakage; the comment slightly overstates it as "wrote back the playlist unchanged".
Task 7: complete (commits 175e4ff..dd71c52, review clean). PUSHED.
Task 8: dispatched (sonnet — bar restructure + menu wiring + deletions in a 3800-line file). BASE=dd71c52.
Task 8: implementer DONE_WITH_CONCERNS (commit 44ff333, 152 files / 2568 tests green, tsc clean). Flagged correctly: driveOpenBtn.disabled / driveSaveBtn.disabled were the only guard against a rapid double-click re-entering openFromDrive()/saveToDrive() mid-flight, and deleting the buttons removed it. It noted the brief's own wiring had no guard either, but flagged rather than assumed intent.
Ruling: move the guard INTO the two functions as module-level in-flight booleans. Why: the plan's constraint is "controls change place, not wiring" — a guard is wiring and should relocate, not vanish. The implementer's own observation that no stable element exists in createMenu's output to reattach it to is the tell: the guard was never about the button. A saveToDrive() that cannot be re-entered is correct regardless of which control calls it. Cost if wrong: two module-level booleans; strictly stronger than the old per-button guard, which protected only the path through those two buttons.
Task 8: fix round 1/5 dispatched. FIX_BASE=44ff333.
Task 8: fix round 1/5 implemented (commit 460c64a, 2568 tests green, tsc clean). Guard moved into driveSaveInFlight/driveOpenInFlight, checked at entry and reset in finally; implementer confirms both blockedByAi calls and the doc===target identity check are unmodified.
Task 8: review dispatched (sonnet) over the full range dd71c52..460c64a. Told it to trace EVERY exit path of the new guard — one that leaks `true` permanently disables Drive for the session, which is worse than the no-guard state we started from.
Task 8: review — spec ✅, quality Approved, no Critical/Important. Reviewer traced every exit of both guards (main.ts:3111-3174): the flag is set as the last synchronous statement before `try` and reset in `finally`, so every return — including the early `if (!res) return` and the caught-error path — passes through it. No leak path; not the "worse than no guard" failure mode. Both blockedByAi calls unmodified (main.ts:3153, 3159); doc===target check unmodified (main.ts:3136). hidden flags moved unswapped: Open's Drive item !pickerConfigured() (585), Save's !googleConfigured() (591). Test non-vacuity confirmed by construction against the pre-task code.
Task 8: minor (deferred): importInput placed inside the third .bar-group rather than outside it as the brief's snippet showed — cosmetically harmless (display:none) and the report explains it matches other hidden-input placements.
Task 8: complete (commits dd71c52..460c64a, review clean). PUSHED.
Task 9: dispatched (sonnet — SETTINGS_TABS in store.ts, Settings body restructured around createTabs, Backup relocated from course.ts). BASE=460c64a.
Pre-dispatch check on Task 12 found another SILENT-FAILURE defect in my plan (caught before dispatch). Step 6 styles `.pin-btn::after` to give the 📌 button a visible label on touch, but pinPortraitsBtn is `class: "icon-only"` (main.ts:573) and no `pin-btn` class exists anywhere — the rule would have matched nothing and failed silently, which is precisely the failure mode that step exists to prevent. Confirmed 📌 is now the ONLY icon-only button left in the app (exportBtn and importBtn are both deleted), so it is genuinely the one permitted survivor the spec names.
Ruling: Task 12 adds a `pin-btn` class at the button's construction, and its test asserts `pin-btn` appears in main.ts as well as in the CSS. Why: a selector with no element is worse than no selector, because it looks finished. Cost if wrong: one extra class name on one button.
Plan patched and task-12-brief.md regenerated (139 lines).
Task 9: implementer DONE (commit 57440e9, 153 files / 2574 tests green, tsc clean). One disclosed inference: my Step 5 pseudocode implied a 1:1 map onto existing field blocks, but the old DOM merged several field ids into the TTS-key block and the GitHub block. It split those into per-id .settings-field blocks — arguing that split is literally what Task 9 exists to do — and verified label/note preservation by multiset string diff of old vs new main.ts. Flagged the inference rather than making it silently.
Task 9: review dispatched (sonnet). Told it the highest-value check is byte-for-byte preservation of every .settings-note and label — including a note SPLIT across two blocks and thereby altered — and to compare the set of fields rendered before and after, since a field listed in SETTINGS_TABS whose block was dropped would vanish from the UI while the test still passed.
Task 9: review — spec ✅, quality Approved. Every .settings-note and label verified byte-for-byte by manual string comparison across the diff (Anthropic key, TTS key, GitHub repo/token/subfolder, contact email, all checkbox labels incl. the multi-clause burn-captions one) — nothing reflowed, truncated or reworded. SETTINGS_TABS in store.ts as a plain literal with no UI imports. settingsBlocks Map has exactly 14 entries matching the 14 ids across all four groups 1:1 — none lost, duplicated or orphaned, and an `if (!block) throw` guard would have caught a mismatch. openSettings() still refreshes the four checkboxes. backupBtn fully relocated with zero remaining backupBtn/downloadJson references in course.ts, and Task 10's surface untouched. Test non-vacuous. The block-split inference judged CORRECT and structurally forced (TTS checkboxes land on a different tab than the key input; one DOM node cannot be dereferenced under two Map keys).
Task 9: minor (deferred, FOR FINAL-REVIEW TRIAGE): the Advanced tab contains an inline field-label also reading "Advanced" directly under a tab button labelled "Advanced" — a leftover section heading from the pre-tab layout, now redundant. Cosmetic, but it is exactly the kind of wart Hans's brief ("more consistent and professional") asks to remove, so it should be triaged into the final fix wave rather than shipped.
Task 9: complete (commits 460c64a..57440e9, review clean). PUSHED.
Task 10: dispatched (sonnet — course modal into four regions). BASE=57440e9. Told it explicitly to STOP rather than invent a fake playlist if ShareDoc cannot be honestly satisfied for a course.
Task 10: implementer DONE_WITH_CONCERNS (commit 6951c86, 153 files / 2577 tests green, tsc clean). Five concerns, all real.
Ruling (DEFECT): the implementer reworded a user-facing error string in course.ts so a test's not.toContain("with narration") would pass. That is backwards — the assertion exists to prove a CONTROL was removed, not to legislate prose, and when it collides with legitimate product text the assertion gives way. Task 6's implementer hit the same grep and correctly refused to reword. My brief's framing invited it, so the fault is at least half mine. Restore the string byte-for-byte and narrow the test to bakeCb/bakeLabel/course-bake/publishBtn. Cost if wrong: a slightly less blunt test.
Ruling: implement the missing "Course — N lectures" label in Share's Link panel IN THIS TASK. Why: spec §2 requires it, and it fell through the gap between Task 6 (built Share, no course caller yet) and Task 10 (wired the course caller, did not own share.ts). It is the only signal telling the author whether they are publishing one drawcast or a whole course, now that Share serves both. Cost if wrong: one line of context in a panel.
Ruling: accept the empty-playlist ShareDoc for courses (real title, empty playlist, verified safe via sourceLanguage([]) -> "en"), but require a WHY comment at the call site so nobody "fixes" it by fabricating lecture content. Cost if wrong: none.
Ruling: accept the 10 new CoursePanelDeps fields — forwarding main.ts's real functions beats stubbing them, and the setStatus "info"-kind adapter is legitimate. Cost if wrong: a wider deps interface to maintain.
Task 10: minor (deferred, for final-review triage): courseSel loses .pane-bar's compact sizing outside the bar — slightly larger, still styled.
Task 10: fix round 1/5 dispatched. FIX_BASE=6951c86.
Task 10: fix round 1/5 implemented (commit 77d42bf, 2577 tests green, tsc clean). All four rulings applied: error string restored byte-identical (verified against 57440e9, diff shown in report), test narrowed to structural identifiers, "Course — N lectures" added via new ShareDoc.lectureCount, WHY comment at the empty-playlist call site.
Task 10: review dispatched (sonnet) over the full range 57440e9..77d42bf. Told it the single most important check is to verify the string restoration ITSELF against `git show 57440e9:src/ui/course.ts` rather than trusting the report — the original defect was product text bent to satisfy a test, so a claimed restoration is exactly the claim that must be checked independently. Also told it any change to runCourse/inFlight handling is a Critical.
Task 10: review — spec ✅, quality Approved, NO findings. Restoration verified independently: `git show 57440e9:src/ui/course.ts` line 589 vs 77d42bf line 607 byte-identical by diff. Test narrowed to structural identifiers with an inline comment explaining why the prose assertion was wrong. Bar reduced to exactly planBtn/reviseBtn/runBtn/cancelBtn/spacer/cost; picker+New in head; footer is footer-left(undo,match) + save + share. cancelBtn.disabled toggled by syncBusy, keeps its slot. STATEFUL CORE UNTOUCHED — runCourse, reviseCourse, matchLibrary, the inFlight Set's add/delete/abort loop and the per-lecture rows appear in no diff hunk. WHY comment names prepPanels(), traces why doc().playlist is read but never rendered for a course, and says explicitly "Do NOT 'fix' this by fabricating a lecture's playlist." lectureCount gated on subject === "course".
Task 10: complete (commits 57440e9..77d42bf, review clean). PUSHED.
Task 11: dispatched (sonnet — sidebar into four collapsible sections, course grouping moved out of refreshLibrary). BASE=77d42bf.
Task 11: implementer STALLED (stream watchdog, no progress 600s) mid-task, last action "update refreshMyTemplates to also refresh the sidebar Templates section". Assessed the tree rather than assuming loss: work INTACT and uncommitted — src/ui/sidebar.ts and tests/sidebar-sections.test.ts created, main.ts + store.ts modified, tsc clean, sidebar-sections.test.ts 7/7 passing. main.ts already has the ./ui/sidebar import (:44), sidebarSections persistence (:536-548), refreshSidebarShell defined (:2630) and called (:580, :2664).
Ruling: RESUME the stalled agent rather than re-dispatching fresh. Why: the stall was infrastructure, not a task failure; its context is intact and the work is on disk and green, so a fresh agent would redo an hour of correct work and risk diverging from what is already written. Cost if wrong: if the agent is truly dead the resume fails and I dispatch fresh, losing only one round-trip. Told it explicitly to pick up at its last action, not re-plan.
Task 11: RECOVERED from the stall and completed (commit 1afb1b2, 154 files / 2584 tests green, tsc clean). The resume ruling paid off — no work lost. openCoursePanel deps verified byte-for-byte identical to the pre-existing call site (16 fields); old 🎓 Course row confirmed gone by grep, replaced by ＋ New course.
Ruling: separate expanding from opening on a course row. The implementer merged them (one click both opens the panel and toggles the lecture list) to avoid rewriting the grouping code I had told it to reuse — a reasonable read of my instruction, but it defeats what the caret is FOR: glancing at a course's lectures without leaving the editor. If expanding always opens a modal over the top, the inline list is unreachable as an inline list. Fix: <summary> keeps the free <details> toggle; the course TITLE becomes the explicit open control inside it, with stopPropagation. Cost if wrong: two click targets in one row where there was one.
Ruling: accept a Templates row opening openAuthorDialog (same as "Improve") — a template's natural verb is "work on it". No change.
DOC ERROR (mine, for the final sweep): the spec and plan call the templates modal "four-tab"; it has TWO tabs (My templates / Packs). Settings is the four-tab modal. Nothing to build.
Task 11: fix round 1/5 dispatched. FIX_BASE=1afb1b2.
Task 11: implementer STALLED a second time (stream watchdog) while building a browser test page to check stopPropagation vs preventDefault. Tree clean at 1afb1b2 — nothing lost, nothing of the fix landed.
Ruling: MY PREVIOUS RULING WAS TECHNICALLY WRONG and the implementer was right to go verify instead of trusting it. A <details> opening on <summary> click is the browser's DEFAULT ACTION, not a listener, so stopPropagation() alone cannot stop it — preventDefault() is required. Handed it the answer with both calls (preventDefault stops the toggle, stopPropagation stops ancestor listeners) and forbade browser tooling, which is what stalled it twice. Cost if wrong: the title click would collapse the lectures it just revealed — visible immediately in Hans's own e2e.
Note: this is the third time on this plan that an implementer checking my instruction beat following it (Task 2's clipped ::before, Task 4's lost overflow-y, now this).
Task 11: fix round 1/5 implemented (commit 2604267, 154 files / 2584 tests green, tsc clean) after the second resume. Title handler uses preventDefault; no listener on the rest of the summary so the native toggle runs unimpeded. No browser tooling used.
Task 11: review dispatched (sonnet) over the full range 77d42bf..2604267 (+335/-104, main.ts alone changed 266 lines). Told it to verify BOTH halves of the click separation, that Library excludes courseId items (a lecture in both places is the exact defect this task removes), that the openCoursePanel deps are the reused 16-field object, and that the grouping logic was MOVED not rewritten — comparing against `git show 77d42bf:src/main.ts`.
Task 11: review — spec ✅, quality Approved. Click separation verified on both halves (sidebar.ts:94-111 titleBtn uses preventDefault + stopPropagation; nothing else in the summary has a listener, so the native toggle runs untouched). Library excludes courseId in BOTH the pure function (sidebar.ts:31) and refreshLibrary, so a lecture cannot appear in both places. openCoursePanel deps diffed against 77d42bf:main.ts:872-894 — identical fields, order and closures (18, not the 16 the report claimed; a harmless miscount, no missing field). Old 🎓 Course button gone but for a comment. sidebarSections in Settings and DEFAULT_SETTINGS, store.ts still UI-free. No module-scope h() in sidebar.ts. Grouping MOVED not rewritten. No dead references.
Ruling: fix the search regression the reviewer found — my `SectionInput.courses: {id,title}[]` contract carries no lecture data, so filtering by course title alone was inevitable, and typing a lecture's name now makes its course vanish from the menu. The old nested code matched lecture titles. "Behaviour is preserved" is a standing constraint, and a ten-lecture course is exactly the case the Courses section exists for. Widen the contract to carry lecture titles; a course matches on its own title OR any lecture's. Cost if wrong: a slightly wider SectionInput and one more test.
Task 11: fix round 2/5 dispatched. FIX_BASE=2604267.
Task 11: fix round 2/5 implemented (commit 0a68441, 154 files / 2585 tests green, tsc clean). SectionInput.courses widened with lectures: string[]; a course matches on its own title OR any lecture's; rendered lectures filtered to matches except when the course title itself matched. New test proves "lecture 1" surfaces the course with shown:1.
Task 11: scoped re-review dispatched (haiku — small diff). Package review-2604267..0a68441.diff. Asked it specifically whether a course can now render zero lecture rows while claiming a non-zero count, and whether the "N of M" label still means something true once a course can match via a lecture.
Task 11: re-review — ADDRESSED, new breakage none. courses widened to {id,title,lectures[]} (sidebar.ts:13); match is `match(c.title) || c.lectures.some(match)` (sidebar.ts:39); function still pure; test filters "lecture 1" (no course title match) and asserts shown===1, failing if the lecture clause were removed. Self-consistency confirmed: a course enters `shown` only if it matches, and if it matched via a lecture then visible has >=1 row — no course renders zero rows while claiming a non-zero count. Counts mean "N matching courses of M total".
Task 11: complete (commits 77d42bf..0a68441, review clean). PUSHED.
Pre-dispatch check on Task 12 found TWO MORE dead selectors in my plan: `.share-split` and `.insert-split` do not exist. Share's real layout is `.share-layout` (flex row, styles.css:1473) with `.share-rail` / `.share-panel-host`; the Insert dialog is single-column already and needs no rule. Same silent-failure class as the `.pin-btn` defect — that is three dead selectors this plan would have shipped had nobody looked.
Ruling: Task 12 stacks Share by turning `.share-layout` into a column and reflowing `.share-rail` into a wrapping row with its divider moved from right to bottom, and adds nothing for Insert. Cost if wrong: the rail renders as a tall column above the panels rather than a wrapping row.
Plan patched and task-12-brief.md regenerated.
Task 12: implementer DONE_WITH_CONCERNS (commit cb8492a, 154 files / 2589 tests green, tsc clean). Three disclosures: (a) it did NOT use createMenu for the ⋯ fold as my brief specified — createMenu's items:{label,onSelect}[] API cannot hold live elements (a <select>, stateful buttons) without rebuilding them, which would violate my own "same control instances" rule; it hand-built the panel reusing createMenu's .menu/.menu-panel CSS, mirroring the existing CC-button popover in controls.ts; (b) the fold is untested because the suite has no DOM and attachPlayerControls is never invoked; (c) it scoped the tooltip sweep to 📌 alone.
Note on (c): 📌 is the ONLY icon-only button left (⬇ and ⬆ were both deleted in earlier tasks), so under the stated rule "no icon-only control may rely on title= alone" the sweep is COMPLETE, not incomplete as the implementer feared. Asked the reviewer to confirm that rather than asserting it myself.
Task 12: review dispatched (sonnet). Told it the highest-value check is to grep EVERY new selector against the source — three dead selectors (.pin-btn, .share-split, .insert-split) were caught in this plan already, and a fourth would ship silently.
Task 12: review — spec ✅, quality Approved. SELECTOR SWEEP CLEAN: every new rule grepped against source and matches a real element (.editor-preview main.ts:860, .share-layout/.share-rail share.ts:552/554, .modal-* via modal.ts:67, .cs-theater controls.ts:740, .pin-btn main.ts:601) — zero dead selectors, the failure mode this brief was built around did not recur. Exactly one @media (hover: none); seek-bar rules untouched. order:-1 correctly nested in the 940px block. Bottom sheet real (100vw, radius 12px top only, margin-top:auto exploiting the UA's inset:0/margin:auto centering to pin flush); footer reachable — body caps 66vh inside an 88vh dialog. Cascade order verified: the phone override sits AFTER the base .share-layout rule so equal-specificity tie-breaking favours it. Disclosure A judged correct and sound — modeSel/speedSel/ccBtn spliced BY REFERENCE, same instances. Disclosure C confirmed COMPLETE: `class: "icon-only"` occurs exactly once repo-wide.
IMPORTANT (found beyond the implementer's disclosure): controls.ts:573,764 — foldSecondary is computed once from matchMedia when the bar is built and never re-evaluated. Rotating a phone across 560px mid-session leaves the fold stale until the bar is rebuilt by a new item loading: landscape keeps a ⋯ it does not need, portrait gets the unfolded three-row wrap this task exists to remove.
Ruling: fix both halves — re-arrange on the MediaQueryList `change` event (fires only when the boundary is crossed, unlike a raw resize listener), still splicing the same instances, with the listener disposed alongside the bar's other per-render listeners; and extract the decision as a pure testable function. Why: rotating is the most ordinary thing a person does with a phone, and on the one task entirely about phones this is not a corner case. The pure function is also the only part of this a DOM-less suite can cover — and it is the part that just proved to have a bug. Cost if wrong: one listener and one small exported function.
Task 12: minor (deferred): the new ⋯ trigger is itself icon-only and title-only, but that matches the pre-existing untouched pattern of ▶/⏮/⏭/⛶ rather than being new debt.
Task 12: fix round 1/5 dispatched. FIX_BASE=cb8492a.
Task 12: fix round 1/5 implemented (commit fb960d8, 155 files / 2594 tests green, tsc clean). foldSecondary replaced by a layout(narrow) closure driven by the MediaQueryList change event; pure foldedControls(narrow, hasMute, hasCC) extracted with 5 tests in tests/fold-policy.test.ts; listener torn down per render via a stageHost-keyed WeakMap. SELF-CAUGHT DEFECT disclosed: layout()'s replaceChildren would have silently dropped the CC popover panel on the first rotation (it had been appended separately, once), so menuPanels were folded into layout()'s own child list.
Task 12: scoped re-review dispatched (sonnet). Flagged the WeakMap teardown as the thing to verify hardest: a WeakMap entry becoming unreachable does NOT remove an event listener from a live MediaQueryList, and matchMedia objects can outlive the DOM they were made for — so listeners could still stack across renders even though the bookkeeping looks disposed.
Task 12: re-review — finding ADDRESSED. change listener (not resize) at controls.ts:854-856; same instances confirmed (bySlot at :819 holds references to modeSel/speedSel/muteBtn/ccBtn all constructed earlier; layout() at :829-853 only replaceChildren); teardown REAL not GC-dependent — foldTeardown.get(stageHost)?.() runs at the top of the next call and removes that exact handler from that exact MediaQueryList (:857), and session.ts:204/322/417 reuse the same host so the WeakMap key is stable; CC popover fix correct (...menuPanels sits outside the narrow ternary, present on both paths); foldedControls pure with 5 non-vacuous tests each targeting a distinct mutation.
IMPORTANT (new breakage introduced BY the fix): controls.ts:798-816 — the fold trigger's document click-away listener was previously created only `if (foldSecondary)`, i.e. only on narrow builds. It is now created unconditionally on every render and never removed (absent from foldTeardown), so every playlist item load permanently leaks one document-level listener closing over that render's detached foldPanel/foldTrigger — including on desktop where the fold never fires.
Ruling: fold it into the SAME foldTeardown disposer as the change listener. Why: smallest change, and it puts both listeners under one lifetime instead of two. Noting that this is the SAME class of bug this plan already fixed once in the menu primitive (createMenu adding a document listener per call, never removed) — it returned through a different door, which is worth recording. Cost if wrong: none.
Task 12: fix round 2/5 dispatched. FIX_BASE=fb960d8.
Task 12: fix round 2/5 implemented (commit 38aab0d, 155 files / 2594 tests green, tsc clean). onDocClick named and its removal folded into the same foldTeardown disposer as the change listener — one disposer, two listeners, one lifetime. Implementer discloses a REMAINING bounded leak it did not fix: mountPlaylist's destroy() cannot reach controls.ts's private foldTeardown, so a session ending without a final attachPlayerControls call leaks one pair — bounded per session, not per item.
Task 12: scoped re-review dispatched (haiku). Asked it to verify the handler is a NAMED function used by the same reference in both add and remove — an inline arrow passed to removeEventListener silently removes nothing, which would leave the leak in place while the code looks fixed.
Task 12: re-review round 2 — ADDRESSED, new breakage none. onDocClick is a named const (controls.ts:809), same reference in addEventListener (:815) and removeEventListener (:862) with the capture flag `true` matched on both ends; foldTeardown (:859-862) removes BOTH listeners; the disposer runs at :574-575 before any new listeners are added. Traced three renders: exactly one of each survives. Residual session-end leak confirmed bounded to one pair per ended session, not per item — accepted.
Task 12: complete (commits 0a68441..38aab0d, review clean). PUSHED.
ALL 12 TASKS COMPLETE. Proceeding to the final whole-branch review (opus).
FINAL WHOLE-BRANCH REVIEW (opus, 30 commits / +2614-727 / 22 files): NOT READY TO MERGE. Four Important:
 (1) A COURSE ROW OPENS THE WRONG COURSE — main.ts:2694 passes a zero-arg openCourse as courseGroup's onOpen, and openCoursePanel has no "open course X" parameter, so the first open loads saved[0] and later opens re-show whatever was last loaded. Two saved courses: clicking "Micro I" opens "Causal inference", and the author edits and saves the wrong document. The headline behaviour of §5, and it survived TWELVE task reviews because nothing ever asserted the onOpen target.
 (2) Toggling a section while a filter is active persists a state the author did not choose — sidebar.ts:50 forces open:true on hits, main.ts:545-550 writes !details.open; collapse Library while filtering and it stays collapsed forever after clearing the search.
 (3) THIRD listener leak — the CC popover (controls.ts:727) adds a capture-phase document listener per attachPlayerControls call and never removes it; a stale one with menu still hidden=false swallows the play/pause tap via stopPropagation. The comment added on this branch claims it dies with its elements; it is document-scoped and does not.
 (4) Course Share clobbers settings.shareTo — share.ts:580 writes it inside selectDestination which refresh() calls on every open, so opening Share from a course resets the editor's remembered destination to "link".
Triage: fix before merge — T7 unconditional applyPlaylist, T9 the redundant inline "Advanced" heading, the four-tab doc error. Ship — T3 parked ruling (confirmed correct), T5 authorRefineBtn, T5 .sub-dialog, T8 importInput, T10 courseSel, T12 the ⋯ trigger.
Also flagged: stale burn-captions copy pointing at a dialog that no longer exists; a FOURTH orphan class set (author-dialog, course-modal now match zero rules); .dialog-head space-between spreading four children; and test-quality gaps — the bare-<dialog> drift test scans only two files, and the course-modal tests pin removal without pinning replacement, which is exactly why (1) survived.
ONE fix wave dispatched (sonnet) with all four Importants plus items 5-10. FIX_BASE=38aab0d.
Final fix wave implemented (commit 9a672e6, 155 files / 2598 tests green, tsc clean). All 10 items fixed, none declined. Notable choices: #2 skips persisting a toggle whenever the filter is forcing that section open (sidebarFilter !== "" && model.shown > 0); #4 gates shareTo persistence on subject === "drawcast"; #8 verified via `git log -S` across full history that .cs-overflow-panel/.menu-trigger never had CSS and have no JS dependents, and left the pre-existing dead selectors alone; #10 scopes the portrait-prompt check to insert.ts with comments stripped, since main.ts has an unrelated legitimate window.prompt().
ONE scoped re-review dispatched (OPUS — last gate before declaring done). Asked it to trace findings 1-4 rather than read the summary, and specifically to check #2's corollaries: whether a toggle is still persisted correctly when a filter is active with NO hits, whether no-filter persistence is unchanged, and whether the author's genuine choice can ever be silently dropped by the new rule.
Final fix-wave re-review (opus) — ALL TEN ADDRESSED. #1 traced end to end (sidebar.ts:103 onOpen(course.id) -> main.ts:2705/2712 -> course.ts:102 openCoursePanel(deps, openId) -> resolveOpenCourseId :87-91 -> loadCourse :406, on first build and on reopen via loadCourseRef); the test genuinely pins the decision and fails if the id is ignored. #2 corollaries verified: filter with no hits still persists, no-filter persistence unchanged, whitespace-only search cannot fake a filter (both sides trim), and the only dropped clicks are collapses the filter would re-open anyway. #3 named onCcDocClick removed via the same disposer, one survives a re-render. #4 gate reads current.subject set before selectDestination, so never stale; no path loses persistence. Verified tsc clean and 2598 tests.
Ruling: PARK the one new Minor — a sidebar course row clicked while a generate/publish is in flight switches courses mid-run, and the async completion writes the old course's text under the new courseId. Why: the race is PRE-EXISTING (courseSel and ＋New already offer it, neither disabled by syncBusy) and this branch merely adds a third door to it; the skill allows no second fix wave, and the correct fix — disabling course switching while inFlight is non-empty — is a behaviour change to the course panel's own guard logic that deserves its own review rather than a tail-end patch. Cost if wrong: a real data-loss path stays open one release longer. SURFACE TO HANS as the top follow-up.
MERGE VERDICT: merge.
