# Ledger — tidy-up + publish round, started 2026-09-01

- 2026-09-01 — Task 3 (A8, promote button + References tab behind developerMode): `src/ui/tabs.ts` doesn't exist — `createTabs`/`Tabs` actually live in `src/ui/modal.ts`. Its `Tabs` interface only exposed `{ el, show(id) }`; the per-tab `<button>` elements are private inside the `createTabs` closure (a `Map`), and no `body.dev-mode` CSS rule exists anywhere in `styles.css` today — every other developer-mode gate (`ratingBox.hidden`, `dataRow.hidden`) is JS-driven, not CSS. So neither of the brief's two preferred options was clean: no exposed buttons for a `hidden` toggle, and no established CSS-gate convention to extend safely (plus a CSS rule would need a selector unique to the References button, which the shared `.tab-btn` class doesn't offer without restructuring `createTabs`). Took the brief's third option: added `setHidden(id: string, hidden: boolean): void` to the `Tabs` interface and its `createTabs` implementation (looks up the button in the existing `buttons` Map and toggles `.hidden` on it — mirrors the JS-hidden idiom already used everywhere else). `applyDeveloperMode` now does `promoteBtn.hidden = !on; instructionsTabs.setHidden("references", !on); if (!on) instructionsTabs.show("instructions");` — the unconditional `show("instructions")` on turn-off is a harmless superset of "only switch away if References was active" (there are only two tabs, and it's a no-op if Instructions was already showing), and avoids needing an `activeId()` getter the `Tabs` interface doesn't have.
- 2026-09-01 — Task 4 (A5, Player/Editor pill → sidebar `▶ Player` row): Step-1 grep confirmed `switchBtn` (the player's own `✎ Edit` escape, `main.ts` ~2016) uses class `cs-bar-btn`, never `mode-btn` — safe to delete all `.mode-btn`/`.mode-toggle` CSS. Deleting `playerModeBtn`/`editorModeBtn` also required deleting their `.active`-class-toggle lines inside `showMode()` and their two `addEventListener` calls (dead references once the consts are gone) — the new sidebar row has no `.active` state of its own, matching the brief's literal snippet, which doesn't toggle one either. Per the brief's "delete that assertion block or repoint it" choice for `tests/palette.test.ts:278-284`, deleted outright (no other rule needs that specific regression coverage; the broader dark-mode sweep test at line 237 still guards the general bug class). Also patched three now-stale prose comments that named the deleted pill/class as if it still existed: the `switchBtn` comment in `present()` ("the editor already has the Player/Editor pill in the topbar"), the mobile topbar comment ("the mode pill and the two round buttons"), and a `palette.test.ts` comment pointing at "the `.mode-btn.active` regression test below" (which no longer exists) — left the older Round-2-fix historical narrative in `palette.test.ts` (~194-208) untouched since it documents past-bug history, not current code. `npm test` (166 files/2699 tests) and `npx tsc --noEmit` both green.
- 2026-09-01 — Task 5 (A1+A2+A4, bar order + Publish move + rename): Step-1 read of `main.ts` ~1004-1040 found the real markup didn't match the brief's literal target string at all — no `YAML▾`/`↻` button exists in either pane-bar today (the ↻ re-render button was removed in an earlier round in favour of automatic re-render; its comment at `main.ts:2795` is the only trace). Traced `S §7.1`/`P §2,§6` to `docs/superpowers/specs/2026-09-01-style-and-vocabulary-design.md` and `.../2026-09-01-publish-design.md` — both confirm `YAML▾ ↻` describes a *future*, not-yet-built state; this task's actual scope (both docs + the brief's Step 4) is only the two-group reorder (Open/Save before Insert) and the Publish move/rename, so I implemented exactly that and did not invent new YAML/↻ buttons. The spec bar's trailing `pane-spacer` (previously there to push `shareBtn` to the right edge) had nothing left to push once `shareBtn` moved out, so I deleted it rather than leave a no-op flex child — no test pinned it either way.
  The brief's own suggested drift-test regex (`/h\("div",\s*\{\s*class:\s*"pane-bar"[^]*?\)/g`) is broken against the real (single-line) markup: the lazy `[^]*?\)` stops at the first `)` it meets, which is the nested `h("span", { class: "pane-spacer" })`'s own close, not the outer call's — so it would silently truncate before ever reaching `reviewBtn`/`shareBtn`. Replaced it with a small depth-counting `balancedCall()` helper in `tests/publish-placement.test.ts` that extracts each full `h("div", { class: "pane-bar" }, …)` call regardless of formatting, plus a `bars.length >= 2` guard test so the scan itself can't silently match nothing. Verified this mattered: ran the brief's literal test text first and confirmed it produced the wrong (truncated) extraction before writing the balanced version. Also discovered the brief's suggested "Insert follows Open/Save" test (`main.indexOf("openMenuHost")` vs `main.indexOf("insertMenu", openIdx)`, both searched across the *whole file*) is trivially true even in the pre-fix state, because `insertMenu`'s declaration (`main.ts:657`) sits before `openMenuHost`'s declaration (`main.ts:782`) — so a file-wide index compare can never see the two groups' order *within* the bar. Rewrote it to search inside the extracted spec-bar text only.
  Grepped `"Share"` across `src/` per the brief's last bullet and drew the line at genuinely user-facing, rendered copy: changed the settings-panel hint at `main.ts:1375` ("Share's YouTube panel" → "Publish's YouTube panel", a `<label>` text node) and `src/ui/course.ts:145`'s `shareBtn` button text ("↗ Share" → "↗ Publish" — a second, separate button in the course panel that opens the same `openShare()` modal, not mentioned in the brief's file list but caught by the grep instruction). Left every `Share`/`ShareTo`/`ShareDeps`/`shareTo`/`migrateShareTo` identifier, JSDoc comment, and the `.share-modal` CSS class alone — those are internal wiring the brief explicitly said stays untouched (`id` stays `"link"`, `settings.shareTo` and `migrateShareTo` untouched), and renaming them would be scope creep with no test or user-facing payoff. Also left `src/ui/course.ts`'s `"Share now"` results-row label alone: it names a different concept (a casual invitation to share the published README link with your audience) rather than naming the Publish modal/button, so it falls under the brief's "leave unrelated uses" carve-out, same as the Google-Docs "Share" reference in `viewer.ts:97`.
  `npm test` (167 files / 2706 tests) and `npx tsc --noEmit` both green.
- 2026-09-01 — Task 6 (C6, `Insert → Image…` reduced to disk import only): Step-1 read of `tests/portrait-insert.test.ts` found no existing dialog-markup drift test — all six tests exercise the pure `portraitInsert`/`PortraitChoice` path directly and needed no changes (per the brief, `PortraitSource`'s three arms and the pure function stay untouched — YAML still expresses `of:`/`url:`/`strokes:`). Appended the brief's drift test verbatim (RED confirmed: failed on `/portrait-source/` and `/A person's name/` still matching, 6 passed/1 failed) before touching `insert.ts`.
  Implementation matched the brief's literal Step 4 almost exactly: deleted `nameRadio`/`urlRadio`/`fileRadio`, `nameInput`/`urlInput`, `sourceMode`/`syncSourceMode`, the now-dead `SourceMode` type, and the `type: "radio", name: "portrait-source"` group's three-line settings-field; `fileInput` lost its `hidden = true`; modal title → `"Insert image from disk"`; submit handler keeps only the former `mode === "file"` branch, now unconditional (no more `mode` variable at all), with the probe/`resolvePortraits`-eager-validate branch for name/url deleted along with the now-unused `Spec` type import (`SpecElement` stays — still used by `portraitInsert` and the file branch's `commit` call). `open()`'s reset dropped the four radio/name/url reset lines, keeping only `fileInput.value = ""`. `main.ts:659` label → `"Image from disk…"`.
  One judgment call past the brief's literal text: restructured the submit handler so `current.readPlaylist()` is read inside `traceFromBlob(file).then(...)` rather than before a since-deleted `apply()` closure — the old code had `apply` as a shared helper called from both the file and name/url branches; with only one branch left, inlining it removes a layer of indirection without changing the WHY-comment's guarantee (playlist still read only after the async trace settles, never held across it). Also updated two comments that quoted or matched the dialog's old identity: `insert.ts`'s `explanation` paragraph (now explains the file-only scope AND points users at YAML for name/url, matching the brief's rationale almost verbatim) and the `openInsertPortrait` JSDoc's quoted `"Insert portrait"` → `"Insert image from disk"` (a literal title quote, not just descriptive prose). Left `main.ts:1981`'s "the Insert portrait dialog's default 'Part'" comment alone — it's informal description of what the dialog still does (insert a portrait element), not a quote of the removed title, and outside the brief's file list. Left three historical planning docs (`docs/superpowers/plans/2026-08-31-*.md`, `docs/superpowers/specs/2026-08-31-editor-shell-design.md`) referencing the old three-radio design untouched — they document past-round history, same convention as Task 4's ledger entry.
  `npm test` (167 files / 2707 tests) and `npx tsc --noEmit` both green.
- 2026-09-01 — Task 7 (C5, delete the halftone/poster/line looks): Step-1 grep confirmed the exact deletion list and, critically, that `traceFromUrl`'s photo branch never calls `traceImage` — so once the non-photo branch is cut, `traceImage`/`traceHalftone`/`tracePoster`/`traceLines`/`TraceOpts`/`RasterLike` have zero remaining callers anywhere in `src/` (their only callers were `portrait.ts:123`, deleted, and the now-deleted test describes). Since every remaining line in `src/render/tracer.ts` (all the private helpers: `simplify`, `chaikinClosed`, `grayscale`, `blur3`, `components`, `boundary`, `normalize`, `percentileOf`, `OFFSETS`) existed only to support those four generation functions, and nothing else imports the file (confirmed by grepping `render/tracer` repo-wide), I deleted the WHOLE file rather than leaving an orphaned module — "modify tracer.ts" in the brief meant trim it to nothing, and an empty/near-empty file with no importers is worse than no file.
  `encodeTrace`/`decodeTrace` (in `src/spec/trace.ts`, NOT in the brief's file list) both stay, untouched, unlike the file's phrasing might suggest for a moment. `decodeTrace` is unambiguously alive: `src/layout/tier2.ts:537` (the render path) and `portrait.ts:175` (the "strokes already encoded, skip re-tracing" check) both call it directly. `encodeTrace` loses its only *production* caller (`portrait.ts:123`, the deleted non-photo branch) but I kept it because (a) `spec/trace.ts` was deliberately absent from the brief's "Files: Modify" list — a strong signal the codec module itself is out of scope; (b) `encodeTrace` is a generic wire-format serializer for `PortraitTrace` shapes (line/fill/wash/paper/dot), not a "look" — it doesn't know or care whether a poster tracer or a hand-built fixture produced the shapes; and (c) deleting it would force every decode-path test (`tests/portrait-trace.test.ts`'s "trace codec" describe, `tests/portrait-element.test.ts`'s `TRACE`/`dotted` fixtures) to hand-encode `t2:` base64url strings by hand instead of round-tripping through the paired encoder — strictly worse tests for a path the brief explicitly keeps alive.
  In `tests/portrait-trace.test.ts`: deleted the `poster look`, `line look`, and `halftone look (the new default)` describes plus their `darkSquare()`/`gradient()` raster-fixture helpers (all `traceImage`-only). The halftone describe's last test, "photo strings round-trip and reject non-image payloads", tests `encodePhoto`/`decodePhoto` only — no `traceImage` involved — so I relocated it (unchanged) to a new top-level `describe("photo codec", ...)` rather than deleting it, since it's a genuine surviving photo-path test that happened to be nested under the wrong describe title.
  In `tests/portrait-element.test.ts`: renamed (did not delete) the `"portrait element — halftone dots"` describe to `"portrait element — dot-shape strokes (decode path)"` with a comment explaining why — its one test builds a `dot`-kind trace directly via `encodeTrace` and checks `layoutSpec`'s render output; it never calls `traceImage`/the halftone generator at all, so it's actually a decode/render-path regression test (nothing generates `dot` shapes anymore, but `tier2.ts` must keep drawing them if an old pinned/pasted spec has them — same "decode stays" boundary as the codec). Deleted the two per-look `portraitCacheKey` assertions (lines 118-119, using `look: "line"`/`look: "halftone"`) since `SpecElement` no longer has a `look` field at all — passing one is now a type error, not just a stale test. Updated the two surviving cache-key assertions (Keynes-by-name, url) to the new no-look format (`p${TRACE_VERSION}|name|keynes` / `p${TRACE_VERSION}|url|...`), matching `portraitCacheKey`'s new `Pick<SpecElement, "type" | "of" | "url" | "strokes">` signature (dropped `"look"` from the `Pick`).
  Left `src/engine-element.ts`'s `look: "sketchy" | "clean"` HTML-attribute concept completely alone — same word, unrelated feature (the whole-figure sketchy-vs-clean render style on the `<drawcast-figure look="...">` custom element), confirmed by grep and by `tests/engine_element_attrs.test.ts` still passing untouched.
  Docs left untouched per the brief and the standing "docs are historical records" rule: `STYLE.md:109` (actually about `reveal`, not `look` — the brief's line reference was imprecise but the rule stands regardless) and `docs/2026-08-28-source-element-spec.md`. Confirmed via grep that `src/llm/prompts/compiler-v1.md` (the live system prompt, which WOULD have mattered) has no portrait-`look` references — its `look`/`poster` hits are all unrelated English usage ("look here", never "halftone").
  `npm test` (168 files / 2700 tests) and `npx tsc --noEmit` both green.
- 2026-09-01 — Task 8 (B1, publish destinations the author *can* enable show disabled with the reason): Replaced `DESTS`' single `needs` predicate with two: `offered` (should this row show at all — only `youtube`'s env credential, Google, can say no) and `ready` (is it usable right now), plus a per-row `reason` string. `destinationOffers(caps, subject)` maps every offered row to `{id, label, action, enabled, reason?}` per the brief's exact interface; `shareDestinations` survives as a one-line filter+map wrapper (`destinationOffers(...).filter(o => o.enabled).map(...)`) rather than being deleted — all six of its existing tests passed unmodified once the new logic was in, since "hide when not ready" was always a legal subset of the new "offer disabled, then filter to enabled" behaviour. Kept it (didn't port/delete its tests) because nothing forced the choice either way and the wrapper is one honest line — flagging this here since the brief left it as my call.
  Rail construction in `refresh()`: every offer (not just enabled ones) gets a `.share-dest` rail button now; a disabled one adds `.dest-off` and appends a `.hint` div carrying `d.reason` as a second line inside the SAME button (reusing the existing `.hint` class rather than inventing new CSS for the text styling — only `.dest-off`'s muted label colour needed a new rule). Its click handler branches on `d.enabled`: enabled calls `selectDestination(id)` as before; disabled closes the modal and calls `current.openSettings()` instead — never `selectDestination`, per the brief's explicit "a `disabled` button would swallow the click" reasoning for why this is a real enabled `<button>`, not the DOM `disabled` attribute.
  `selectDestination` now filters `destinations` to `.enabled` before calling `panelVisibility` (so a disabled offer's panel can never be shown even if something called this with its id by mistake) and only marks a rail button `.current` when `d.enabled && d.id === id`. `refresh()` no longer hides the whole `.share-layout` when nothing is enabled — since `link` (and `video` for a drawcast) are now *always* offered, `destinations` itself is never empty, so that old empty-modal branch was dead code once the third state existed. Instead `panelHost.hidden`/`emptyHint.hidden` toggle on whether the ENABLED subset is empty; `emptyHint`'s text changed from the old GitHub-specific "Set your GitHub repository and token in Settings to publish." to the brief's subject-neutral "Publishing needs a destination — the rows above say what each one needs." (kept the existing "Open Settings" button alongside it — harmless one-click fallback now that each disabled row already explains itself and offers the same route individually). Because `emptyHint` renders as a sibling BELOW `.share-layout` (not beside the rail), "the rows above" is literally true in the DOM, not just figuratively.
  Added `.share-dest.dest-off { color: var(--muted); }` and `.share-dest.dest-off .hint { margin-top: 0.15rem; }` right after the existing `.share-dest`/`.share-dest.current` rules in `styles.css` (grepped first per the brief's instruction — confirmed `.share-dest`/`.share-rail`/`.share-panel-host` were the only existing rail classes, so `.dest-off` needed to be new, not reused). `.share-dest.current` stays in `palette.test.ts`'s `RUST_ALLOWED_SELECTORS` allowlist unchanged — `.dest-off` never uses `var(--rust)`, so no allowlist update was needed.
  The `"three panels"` literal-comment test (`panelVisibility`'s own docstring) needed no change: `panelVisibility`'s signature and its "exactly the selected one, and ONLY if offered" contract are untouched — only its callers now pre-filter to `.enabled` before calling it.
  TDD: wrote the brief's five `destinationOffers` tests verbatim into `tests/share-destinations.test.ts` first, confirmed RED (`TypeError: destinationOffers is not a function`, 5 failed/11 passed), then implemented; GREEN at 16/16 in that file. `npm test` (168 files / 2705 tests) and `npx tsc --noEmit` both green after the full implementation (rail/CSS included, not just the pure-function layer).
- 2026-09-01 — Task 9 (B9, the yaml carries its founding prompt): implemented `PlaylistMeta.prompt` exactly as the brief specified — read in `readMeta` (string-typed only), written in `formatPlaylist` next to `title`/`subtitle` (always-when-set, never compared against a default, because `DEFAULT_META` has no prompt to compare with), and added to `isSingle`'s conjunction so a doc that carries a request always serializes WITH its header. Generation writes it in both paths (`generate()` and `generateMulti()`), each as its own `playlist.meta.prompt = rawRequest;` statement rather than inline in the object literal, so both carry the one expression the drift test pins. The revise path is untouched, verified line by line: it copies `prompt: doc.prompt` into the new `Doc` and never assigns to any `meta.prompt` — a source-level drift test now enforces that *every* `.meta.prompt =` write in `main.ts` is `= rawRequest`, so a future revise/translate path writing the instruction there fails the suite rather than silently overwriting the founding request that exemplars and the log still pair specs with.
  **The accepted visible shape change (§F.3.3), confirmed by hand:** a generated single-figure document now opens in the editor as `playlist:` / `  prompt: <the request>` / `---` above the spec — a three-line header where there used to be a bare spec. Verified by rendering a real `singlePlaylist(spec)` + `meta.prompt` through `formatPlaylist` and re-parsing it (scratch test, deleted): the header appears, and the request comes back byte-identical. This is deliberate and is the whole point of B9 — without it `isSingle` short-circuits to `formatSpec(spec)` and the request is dropped on every save.
  **Second visible consequence, not in the brief, fixed:** `main.ts:2142` gated the `👍 Learn from this` (promote-to-exemplar) button on `isSingle(doc.playlist)`. With B9 that predicate goes false for exactly the documents the button exists for — an AI-generated single figure — so the button would have been permanently disabled after every Generate. Repointed it to `itemsOf(doc.playlist).length !== 1`, which is what its own comment ("exemplars are (request, single spec) pairs — a multi-part doc has no such pair") actually means; `isSingle`'s other three callers (`saveDiskFormatSel`'s JSON guard, `saveDiskBtn`'s format pick, `autosave`/`share.ts`'s "store the playlist text or just the spec" branch) all *want* the new answer: a document with a header genuinely cannot be JSON, and storing its full text in the library is how the prompt survives there too.
  Also reworded the JSON-refusal status line (`main.ts:699`) from "Playlists are YAML-only …" to "This document has a playlist header, so it is YAML-only …" — the old wording read as a lie in front of a one-figure document that now has a header. No test pinned the string (grepped first).
  **Judgment call — no `docPrompt()` helper.** The brief's Interfaces section suggested `const docPrompt = () => doc.playlist.meta.prompt;`. I did not add it: it would return `undefined` for a pre-B9 library entry and for every bundled example (which has a `request` but no header), silently breaking the promote button's exemplar label, `logRevision`'s `${doc.prompt || doc.title}` prefix and `setDoc`'s stack seed. `Doc.prompt` stays the single read path, kept honest by the brief's prescribed `doc.prompt = doc.playlist.meta.prompt ?? doc.prompt;` in `setDoc` (meta authoritative when present, library copy as fallback).
  **Judgment call — the same `?? doc.prompt` rule applied at two sites the brief listed but did not spell out.** `showVersion()` (~2110) and `ensureRendered()` (~2840) both rebuild `doc` from freshly parsed text WITHOUT going through `setDoc`, so they were the two places where a hand-edited or restored header could disagree with `doc.prompt`. Both now read `prompt: playlist.meta.prompt ?? doc.prompt`. This is a read of the meta, never a write to it, so the revise rule is untouched.
  **Judgment call — pre-B9 and bundled documents are NOT backfilled.** `docFromSaved` prefers `parsed.meta.prompt` and falls back to `saved.prompt` (per the brief), but does not copy the library's prompt INTO the meta; bundled examples and `initialDoc()` likewise set `Doc.prompt` only. So an old library entry still loses its request on export until it is regenerated. Deliberate: backfilling would sprout a header on every existing document and on every bundled example the moment it loads, which is a much larger shape change than the one §F.3.3 accepted, and the ruling scopes the field to the *founding Generate request*, which a bundled example's `request` field is not.
  **Concern (pre-existing, not introduced here):** a revise round-trips the document text through the model and re-parses its reply, so a model that drops the `playlist:` header drops the founding prompt from the file with it (`doc.prompt` still survives, and a later re-render restores nothing). This is exactly how `title`/`subtitle` already behave on that path, so B9 adds no new failure mode — but if header loss on revise turns out to be real in practice, the fix (re-attaching the outgoing document's meta to the incoming playlist) would be a *write* in the revise path and needs its own ruling, since the drift test now forbids it.
  TDD: wrote the brief's three tests verbatim into `tests/playlist.test.ts` plus three of my own (prompt alongside a real header; a non-string `prompt:` ignored; and a hostile-string round trip), and the four drift tests into `tests/publish-placement.test.ts`, then confirmed RED (6 failed / 42 passed) before touching `playlist.ts`. The hostile-string test earned its place: real requests carry `#playlist`/`#parts=3` tags, and a bare `#` after a space starts a YAML comment — verified js-yaml single-quotes those (and `draw: …`, leading/trailing spaces, `yes`, `- a dash`) and block-scalars a multi-line request, so nothing truncates on the way to disk. `npm test` (168 files / 2715 tests) and `npx tsc --noEmit` both green.
