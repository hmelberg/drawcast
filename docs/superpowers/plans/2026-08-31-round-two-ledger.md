# Round two — execution ledger and decision record

*2026-08-31/09-01. The controller's ledger from executing
`docs/superpowers/plans/2026-08-31-round-two.md` across 8 tasks and 17 commits.
Kept for the same reason as round one's: the rulings below are decisions taken
on Hans's behalf, each with why and what it costs if wrong.*


## Pre-flight probes (run, not assumed)

| Probe | Result |
|---|---|
| `rough.generator().toPaths(drawable)` headless in node | PASS — returns `{d,…}` with real path data, no DOM. Task 8 is viable. |
| `npx vite-node scripts/*.ts` importing roughjs | PASS — vite-node 3.2.4 arrives with vitest, no new dependency. Task 8's script runner. |
| `commitFiles` exported from `publish/github.ts:252` | PASS — `(repo, token, branch, files: {path,content}[], deletions, message, fetchImpl)`. Task 7 is a caller, not new machinery. |
| `joinPath` exported from `course/publish.ts:28` | PASS — Task 7's path builder. |
| `figure-style.ts` reads `var(--ink, #3d3833)` at :77 | CONFIRMED — the dark-mode trap is real, not theoretical. |
| No `prefers-color-scheme` anywhere in styles.css | CONFIRMED — dark is entirely new. |

## Pre-flight conflict scan

### Pairs sharing a file or interface

| Pair | Produces → consumes | Finding |
|---|---|---|
| T1 → T2 | `contrastRatio` in `tests/contrast.ts` | Clean. T2 imports it for the dark ratios. |
| T1 → T2 | both edit `:root` in styles.css | Sequential; T2 adds blocks below rather than editing T1's tokens. Clean. |
| T1 → T3 | T1 restricts `--rust` to `button.primary`; T3 adds an `--rust` edited dot | **CONFLICT** — T3's dot would fail T1's accent assertion. |
| T2 → T3 | both edit `main.ts` startup | Different regions (theme apply vs render policy). Clean. |
| T3 → T6 | `ensureRendered()` called by Save | Ordering fine (3 before 6). Clean. |
| T4 → T6 | T4 deletes `formatSel`; T6 rebuilds the choice in Save-to-disk | Ordering matters and is stated in the plan's own note. Clean. |
| T4 → T6 | T4 must NOT delete `Settings.specFormat` | Stated explicitly in T4 Step 3. Clean. |
| T6 → T7 | `Doc.sourcePath` added in T6, used in T7 | Clean, deliberate split. |
| T6 → T7 | both edit the Open/Save menus | Sequential; T7 appends two items. Clean. |
| T5 → T8 | T5 removes `pinPortraitsBtn`'s icon-only class; T8's world has no icon-only left | Clean. |
| T8 → T1 | T8 deletes `.squiggle`, which T1's accent rule also touches | T1 leaves `.squiggle` alone (it is deleted, not recoloured). Clean. |

### Self-consistency, per task

| Task | Its tests vs its code | Finding |
|---|---|---|
| T1 | parses `:root` hex tokens; asserts three ratios | Clean. The two failing numbers are pre-measured (1.33, 3.45). |
| T2 | regex over the dark block; figure-token absence | Clean. |
| T3 | pure `needsRender` in `src/render/policy.ts` | Clean — the plan explicitly moves it out of `main.ts`, which cannot be imported in a node test. |
| T4 | `not.toMatch(/formatSel/)` over main.ts | Clean. |
| T5 | menu label + no icon-only 📌 | Clean. |
| T6 | `migrateShareTo` + updated share-destinations | Clean. |
| T7 | pure path/manifest functions | Clean. |
| T8 | determinism + path data + squiggle/favicon absence | Clean. |

## Rulings (pre-flight)

Ruling: Task 3's "edited" dot does NOT use `--rust`. Why: Task 1 restricts the accent to `button.primary` and asserts it, so a rust dot would either fail that test or force it to be weakened one task after it was written — and the plan's own Task 3 Step 7 anticipates the collision by offering to "add the selector to that test's allowance", which is the weakening. The dot is a state indicator; `--muted` with a filled shape reads as state without spending the accent. Cost if wrong: the dot is quieter than it might be; a one-token change.

Ruling: work proceeds on `main`, as in round one. Why: repo convention, and the plan's tasks each end in a commit the controller pushes. Cost if wrong: a revert of a pushed range.

## Progress
Task 1: implementer DONE_WITH_CONCERNS (commit 6e0bbd6, 156 files / 2606 tests green, tsc clean). Measured the pre-fix numbers as instructed: border 1.3369, muted 3.4499 — both match the audit.
Ruling: the implementer was RIGHT and I was WRONG about `--line`. I verified independently: my proposed #c2b9a4 gives 1.610:1 against the new paper, not the 3.0 I asserted; its substituted #8f8265 gives 3.125:1. I measured the diagnosis and then stated numbers for the prescription without computing them. Refusing my value and substituting a correct one from the same warm-khaki family was exactly right. Spec corrected with the reason recorded. Cost if wrong: none — the test now computes it.
Ruling: the accent keeps THREE named roles, not one. Counted myself: 28 var(--rust) uses, not the five my plan guessed. Reducing to literally one is the wrong correction — an accent has legitimate work. Keeps: primary action, "you are here" (current/active), links. Loses: hover (transient, everywhere, and precisely what makes the accent stop meaning anything) and incidental text/controls (.row-note in accent is simply wrong — a note is not an action). Cost if wrong: a slightly busier accent than a purist would want, but a principled and asserted one.
Ruling: the narrowed test becomes an ALLOWLIST test — every selector using var(--rust) must appear in an explicit permitted list. Why: a drift guard that only checks a subset is how 28 uses accumulated in the first place; an allowlist forces a future accent use to be added deliberately. Cost if wrong: one more line to edit when an accent use is legitimately added.
Ruling: accepted adding 0.85 to the type-scale guard list — a guard with a hole in it is not a guard.
Task 1: fix round 1/5 dispatched. FIX_BASE=6e0bbd6.
Task 1: fix round 1/5 implemented (commit aa2fc29, 156 files / 2605 tests green, tsc clean). Allowlist test went 15/25 rule-blocks failing -> 0, with 10 permitted uses remaining. Three uses fit none of my five rows; it demoted all three to --ink (the safe, reversible direction) and flagged them rather than guessing a bucket. It also spotted that .viewer-footer a / :hover were swapped in my table and resolved it via the Hover rule.
Ruling: ACCEPT all three demotions. (a) `.spec-json.streaming` — the AI-writing indicator already animates; motion is the signal and colour is redundant, and adding a fourth accent role for one selector is exactly how 28 uses accumulated. (b) `.sidebar-new` base — the implementer is right that a second full-strength CTA competed with button.primary; the principled answer is one primary style, not two lookalikes, and the button keeps its border and background tint so it still reads as distinct. (c) `.cs-infocard-sense` base — sense chips are options, not links; hover already carries the interactive signal. All three are one token to flip back if Hans disagrees, which is what the allowlist is for. Cost if wrong: three controls are quieter than intended, each reversible in one line.
Task 1: review dispatched (sonnet). Package review-4c7cc15..aa2fc29.diff.
Task 1: review — spec ✅, quality Approved. Reviewer INDEPENDENTLY recomputed every ratio from the real tokens: --line/--paper 3.1246:1, --muted/--surface 5.421:1, --ink/--surface 11.40:1 — all clear. Exactly 10 var(--rust) sites remain, all mapping onto the 9-entry allowlist, no orphans. The six type sizes are gone, not aliased (pre-existing unrelated 0.9rem/0.8rem sites correctly left alone). tests/contrast.ts confirmed not collected as a suite. No .ts source file touched — no behaviour change. Both "watch it fail" gates evidenced with real numbers.
IMPORTANT: the allowlist test has a comma-selector loophole — the regex captures the WHOLE selector list as one string and checks `sel.includes(allowed)`, so `.tab-btn.active, .sneaky-new-thing { color: var(--rust) }` passes silently. The reviewer ran the exact test logic and confirmed it. Not exploited today, but the test's own comment claims a guarantee it does not provide.
Ruling: split the selector list on commas and require EVERY part to be allowlisted, and add a case proving a comma-smuggled selector is rejected — lifting the check into an exported helper if needed to make it testable. Why: a guard defeatable by adding a comma is the same class of problem as the 28 uses it was written to prevent, and a guard nobody has seen reject anything is not yet a guard. Cost if wrong: a slightly stricter test that a legitimate multi-selector rule must be listed against in full.
Task 1: fix round 2/5 dispatched. FIX_BASE=aa2fc29.
Task 1: fix round 2/5 implemented (commit f5c580a, 156 files / 2607 tests green, tsc clean). Extracted exported isRustUseAllowed(selectorList, allowedList) splitting on commas and requiring every part to match; verified the OLD logic let `.tab-btn.active, .sneaky-new-thing` through (true) and the new one rejects it (false); added two unit tests (reject-smuggled, still-accepts-legit). @media diagnostic comment added. styles.css untouched this round.
Task 1: scoped re-review dispatched (haiku). Asked it specifically whether the per-part match is EXACT or still substring-based — a per-part .includes() would still let `.new-thing.tab-btn.active` through — and whether a test proves the guard still ACCEPTS the legitimate rules, since a guard that rejects everything is as broken as one that accepts everything.
Task 1: re-review round 2 — ADDRESSED, new breakage none. isRustUseAllowed exported, splits on commas, requires EVERY part via .every(); per-part matching is still substring-based, which the reviewer judged correct and safe (the vulnerability was whole-string includes; a standalone `.sneaky-new-thing` part matches no entry). Rejection test present, and acceptance tests cover both a compound pseudo-selector and a real multi-selector rule. styles.css untouched this round.
Task 1: complete (commits 4c7cc15..f5c580a, review clean). PUSHED.
Ruling (mine, before dispatching Task 2): I computed my own proposed dark palette and found `--line: #4a443b` is 1.803:1 against the dark paper, not the 3:1 the plan claimed. That is the SECOND time this plan asserted a border colour without computing it — the first was Task 1's #c2b9a4, which the implementer caught. Replaced with #79715c (3.58 on paper, 3.23 on surface). Cost if wrong: none; the values are now measured.
Ruling: the dark test must check `--line` against `--surface` as well as `--paper`. Why: borders sit on panels as well as the page, and a paper-only check passes colours that still vanish against a surface — #726a56 would have cleared paper at 3.23 while failing panels at 2.91. Cost if wrong: a slightly lighter border than strictly needed on one ground.
Task 2: implementer DONE (commit dad0b3b, 156 files / 2611 tests green, tsc clean, no concerns). Independently recomputed all four ratios and matched; cut the figure's --ink tie BEFORE adding the dark palette so the broken state never existed; grep confirms only --sketch-font remains in figure-style.ts; applyTheme() runs right after loadSettings() at startup as well as on dialog change.
Task 2: review dispatched (sonnet). Told it the highest-value check is the figure trap, and to look BEYOND figure-style.ts across src/render/ for any other style injection into the figure or the exported frame that might read a chrome token — the export path must be theme-independent, not just that one file.
Task 2: review — spec ✅, quality Approved, NO Critical/Important. THE TRAP IS CLOSED: figure-style.ts has zero chrome colour tokens (only --sketch-font x2); the caption is a bare literal #3d3833, identical in both themes. Reviewer checked the WHOLE export path as instructed, not just that file: `grep -rn "var(--" src/render/` finds nothing outside those two font uses, every drawn element takes colour from the drawing spec (d.style.color/fill), and export/video.ts uses its own hardcoded PAPER/INK literals with no getComputedStyle anywhere — the exported frame is theme-independent. All four dark ratios recomputed with the reviewer's OWN script (not the repo's helper): 3.580 / 3.225 / 5.690 / 12.586. Cascade traced both directions. applyTheme() runs at module top-level right after loadSettings(). All five dark tokens exist on bare :root. No new accent use.
Task 2: minor (deferred): the data-theme block duplicates the media-query hex values rather than deriving them — but that is exactly what my brief's snippet specified, so it is my choice, not the implementer's.
Task 2: complete (commits c85cd8b..dad0b3b, review clean). PUSHED.
Task 3: dispatched (sonnet — the trickiest task: ensureRendered, the Play trap, the edited dot). BASE=dad0b3b.
Task 3: implementer DONE (commit 58f5e0f, 157 files / 2615 tests green, tsc clean). Solved the Play trap with a new `beforePlay` hook in ControlsOptions and an `autoplay` field in SessionOptions — plumbing my brief did NOT specify, and necessary because main.ts does not own the mounted Play button. Path: togglePlay -> beforePlay -> ensureRendered(true) -> present(true) -> mountPlaylist({autoplay:true}) -> session.ts calls hd.timeline.play() on the NEW timeline once the first item mounts; togglePlay returns early so the about-to-be-destroyed timeline is never touched. Pause bypasses beforePlay entirely.
Task 3: review dispatched (OPUS — highest-risk task in the round). Told it to trace the whole Play path itself rather than read the summary, and to check four specific ways it could still be wrong: the old timeline touched after destruction, a new session that mounts but never plays, PAUSE accidentally triggering a remount (which would lose the author's position mid-playback), and a double-play. Also asked it to grep EVERY assignment to specArea.value for a missed lastRenderedText site.
Task 3: review (opus) — spec ✅, quality Approved, 4 Minors. THE TRAP HOLDS, traced in real code: present() destroys the old session SYNCHRONOUSLY before its first await, so the dead timeline is never touched (togglePlay returns early at controls.ts:1046); all three mount paths honour autoplay (session.ts:213 single, :441 title page, :461 mountItem); pause bypasses beforePlay (controls.ts:1039-1042); unchanged text plays with no remount; exactly one timeline.play() per mount path. All 8 specArea.value sites covered — LLM streaming sites correctly leave the dot alone. ensureRendered preserves the whole original handler. Dot uses --muted; palette allowlist re-run 13/13 and untouched.
Ruling: fix all four Minors in one round rather than deferring. Why: two are behaviour regressions THIS task introduced, and the other two are one-liners in the same function — deferring four related items on the round's riskiest change is worse than one extra round. Cost if wrong: one more review cycle on a task that already passed.
Ruling: the viewing-old-version item is NOT pre-existing, correcting the implementer's own claim. The reviewer checked: the disabled ↻ button used to make Pin and Insert silently no-op while viewing; ensureRendered now runs unguarded and appends a manual-edit version + autosave FROM AN OLD VERSION, jumping the cursor to newest. Non-destructive (pushVersion never deletes) but new. Cost if wrong: a guard that makes Pin/Insert no-op while viewing history, which is what it did before.
Ruling: ensureRendered becomes SILENT. Why: "Re-rendered from edited spec." was right for a button press and is noise when it fires on Play/Share/Save/mode-switch, overwriting status the user may actually need. The edited dot is the render's visible signal now. Cost if wrong: no confirmation that an automatic render happened — which is the dot's job.
Task 3: fix round 1/5 dispatched. FIX_BASE=58f5e0f.
Task 3: fix round 1/5 implemented (commit d2bba8a, 157 files / 2619 tests green, tsc clean). Added a pure canRender(viewingHistory, aiBusy) beside needsRender, wired as ensureRendered's first substantive check; removed the status line; moved refreshEditedDot to the top so every early return keeps the dot honest; reused the existing aiBusy flag rather than inventing one. 4 new canRender cases, watched fail then pass.
Task 3: scoped re-review dispatched (sonnet). Told it the way this fix could backfire: a guard that returns false too eagerly silently restores the original "pressing play does nothing" bug, so it must confirm the MAIN path (no history viewing, no AI) still re-renders and autoplays. Also to check aiBusy is actually set during a stream rather than only on some generation paths, which would leave the gate holed.
Task 3: re-review — all FOUR findings ADDRESSED, new breakage none. canRender is ensureRendered's first substantive check (main.ts:2678), strictly before the doc rebuild (2684), pushManualEdit (2685) and autosave (2688). refreshEditedDot is the literal first line (2677), so all three early returns keep the dot honest. The status line is gone repo-wide (only a stale worktree copy remains, out of scope). canRender's second arg IS the pre-existing aiBusy (main.ts:2228) that blockedByAi reads, and setAiBusy(true) precedes BOTH streamIntoSpec call sites — the reviewer checked that shareBtn, beginExport, saveToDrive and the Player mode-switch carry no blockedByAi guard of their own, so this gate is the only protection and has no holes. MAIN PATH CONFIRMED UNAFFECTED: canRender(false,false) is true, so ordinary Play still re-renders and autoplays. Tests non-vacuous by inversion-check against four broken variants.
Task 3: complete (commits dad0b3b..d2bba8a, review clean). PUSHED.
Task 4: dispatched (haiku — small, self-contained removal). BASE=d2bba8a.
Task 4: implementer DONE (commit e2eb6df, 2620 tests green). formatSel and its listener gone; 5 display sites now pass "yaml" explicitly; Settings.specFormat and share.ts's picker left intact for Task 6.
Task 4: review dispatched (sonnet). Told it the two ways this small task could do real damage: deleting Settings.specFormat (which a later task depends on), and a "display site" that was actually a SAVE site being hardcoded to yaml — which would silently change what gets written to a file. Also to confirm Share's Spec download still honours specFormat rather than being hardcoded, since that destination does not disappear until Task 6.
Task 4: review — spec ✅, quality Approved. Settings.specFormat survives (store.ts:100 interface, :141 default). Parsing untouched — spec/text.ts not in the diff, JSON.parse path still first. formatSel fully gone incl. its listener and with no residual guard in ensureRendered. Share's Spec download untouched behaviourally (still driven by settings.specFormat). Test non-vacuous — formatSel verbatim present at d2bba8a.
Task 4: minor (deferred, accuracy not defect): the report called all five changed sites "display formatting", but the fifth is saveToDrive — a WRITE path. The change is nonetheless correct and closes a latent bug: specArea.value is now always YAML, so deriving the Drive filename/mimeType from a stale settings.specFormat of "json" would have written a .json name over YAML content. Reviewer verified saveSpec sends {name, mimeType} on every save.
Task 4: complete (commits d2bba8a..e2eb6df, review clean). PUSHED.
Task 5: dispatched (sonnet — Images menu + Pin's dialog). BASE=e2eb6df.
Task 5: implementer DONE (commit fb98d6d, 157 files / 2620 tests green, tsc clean). Three disclosures: (a) it DELETED a round-one test asserting the 📌 button carried a pin-btn class, plus the dead .pin-btn::after CSS, because this task retires that button entirely; (b) concluded ensureRendered() need not precede Pin's read since specArea.value is always live; (c) removes rather than hides the Pin button when nothing is pinnable so .dialog-footer:empty collapses the bar.
Ruling: the round-one test deletion is CORRECT, verified by me before dispatching the review. `grep -rn 'class: "icon-only' src/` now returns NOTHING, and Pin is a labelled menu item ("Pin all images"). Round one's rule was "no icon-only control may rely on title= alone"; with no icon-only control left anywhere, the guard's subject no longer exists. Deleting it — and flagging the deletion rather than gaming it with a stray class on a button that no longer needs one — was the right call. Cost if wrong: none; the invariant is satisfied more strongly than before.
Task 5: review dispatched (sonnet). Told it the highest-value checks are that Pin's logic MOVED rather than being rewritten (compare against `git show e2eb6df:src/main.ts`; any change to what gets embedded is a Critical), and that the dialog's copy is ACCURATE — that text is the entire point of the task, since the explanation previously existed only in a tooltip the app's own author had forgotten.
Task 5: review — spec ✅, quality Approved, NO findings. Pin's embed logic verified IDENTICAL against e2eb6df: same resolvePortraits/resolveSources, same Promise.all(items.flatMap(...)), same failure counting and status strings, same formatPlaylist->ensureRendered write-back. Only mechanical changes forced by becoming a dialog (contactEmail via a deps getter since insert.ts has no main.ts scope, a disabled guard, dialog.close() on success). Dialog copy matches the required text and does not overstate. "Nothing to pin" moved to open-time with the button REMOVED so .dialog-footer:empty collapses. No new accent use — styles.css diff is a pure deletion. New test non-vacuous against e2eb6df.
Task 5: complete (commits e2eb6df..fb98d6d, review clean). PUSHED.
Task 6: dispatched (sonnet — Open/Save menus, Share loses Spec, shareTo migration, Doc.sourcePath). BASE=fb98d6d.
Task 6: implementer DONE (commit 979782f, 158 files / 2626 tests green, tsc clean). Three disclosures: (a) openMenu items do NOT call ensureRendered, reading the requirement as save-specific; (b) SavedDrawing.sourcePath is optional while Doc.sourcePath is required, to avoid collateral breakage in course-store.test.ts, with docFromSaved normalising; (c) touched ui/course.ts for one saveDrawing() call needing sourcePath: null.
Ruling: (a) the implementer's reading is CORRECT and spec-conformant — spec §4 lists Play, Player-switch, Share, export and Save, and deliberately not Open. Accepted. Recording the related question separately: opening a document replaces the current one, so unsaved edits are discarded; calling ensureRendered first would persist them, but the guarantee would be partial anyway since canRender no-ops while viewing history or streaming. That hazard is PRE-EXISTING, not introduced here — deferring it to the final review to triage rather than expanding this task. Cost if wrong: an author who edits, does not play, and opens something else loses the edit, exactly as today.
Ruling: (b) accepted — an optional field is MORE correct for a persisted shape, since a record written before the field existed genuinely has no value, and docFromSaved normalises on the way in. Cost if wrong: none.
Ruling: (c) accepted — collateral required by the field addition, not scope.
Task 6: review dispatched (sonnet). Told it the migration is the highest-value check precisely BECAUSE CI cannot catch it: a fresh test environment has no stored settings, so a migrateShareTo that is exported and unit-tested but never CALLED inside loadSettings passes every test and breaks only on a machine with a stored "spec".
Task 6: review — spec ✅, quality Approved, NO Critical/Important. THE MIGRATION IS WIRED IN: store.ts:547 `s.shareTo = migrateShareTo(s.shareTo)` runs on every load, with an INTEGRATION test that stubs localStorage with a stored "spec" and asserts "link" comes back — closing exactly the CI-invisible gap. Save-to-disk reproduces Share's Spec panel exactly (identical filename regex, format fallback, formatPlaylist/downloadText). Share's spec destination fully removed with no dangling references. share-destinations.test UPDATED not deleted. Both Save destinations call ensureRendered. Drive paths otherwise untouched — same in-flight guards, same `target = doc` identity capture. Doc.sourcePath round-trips through every path. Disclosure 1 CONFIRMED pre-existing: openFromDrive and the import handler already did not call ensureRendered at fb98d6d.
Task 6: notable — the reviewer found the ORIGINAL share.ts never wrote settings.specFormat back despite its own doc-comment claiming it did (no assignment existed anywhere at fb98d6d). The new Save-to-disk actually persists it, fixing a latent bug rather than adding scope.
Task 6: minor (deferred): src/ui/share.ts:121's comment still says "four panels" and now overcounts. Cosmetic.
Task 6: complete (commits fb98d6d..979782f, review clean). PUSHED.
Task 7: dispatched (sonnet — the new capability: GitHub source save + manifest + open). BASE=979782f.
Task 7: implementer DONE (commit 18c3e13, 159 files / 2639 tests green, tsc clean). Proved the single-commit property with a real test asserting exactly one /git/trees POST and one /git/commits POST whose tree carries BOTH paths — not a comment. The two GitHub status strings are distinguishable and share no substring; neither function touches the other's field (publishedAs vs sourcePath). No store.ts edit needed — Task 6 already added the field and autosave threads it.
IMPORTANT (implementer-flagged, and a defect THIS task introduces): openMenu/saveMenu are built once at module init with `hidden` baked in. Until now every item's hidden depended on ENVIRONMENT config, static per session, so that was harmless. GitHub's repo and token are SETTINGS the user edits at runtime — so a user configures GitHub, goes to Save, finds no GitHub destination, and concludes the feature is broken. Invisible to the suite.
Ruling: fix it in main.ts, NOT in menu.ts — wrap each menu in a stable container and replace its child with a freshly built menu after a settings save. Why: createMenu decides plain-button-vs-dropdown at construction, so making `hidden` dynamic would require the primitive to re-render itself — a bigger change to something four callers depend on and which has already had one round of subtle listener bugs. Apply to BOTH menus; leaving one stale is worse than either state. Cost if wrong: two container spans and a rebuild call.
Task 7: fix round 1/5 dispatched. FIX_BASE=18c3e13.
Task 7: fix round 1/5 implemented (commit b7322c5, 160 files / 2647 tests green, tsc clean). openMenu/saveMenu became buildOpenMenu()/buildSaveMenu() inside stable host spans; refreshCredentialMenus() swaps each host's child. IT CAUGHT SOMETHING MY RULING MISSED: the GitHub token bypasses persist() (its own localStorage key), so refreshing only from persist() would still leave the menu stale after entering just a token — it wired the token input's own listener too. Applied to Drive items as well, not just GitHub's. Added pure src/ui/destinations.ts (openDestinations/saveDestinations over a CredentialState) with 8 tests, and said plainly that the DOM swap itself is not unit-testable here rather than forcing it.
Task 7: review dispatched (OPUS — the only task in this round with remote side effects on the user's own repository). Told it to verify the atomicity TEST rather than the claim (could it pass while two separate commitFiles calls occurred?), to confirm publishing is byte-untouched since it writes to a public repo, and to check whether a tree built from a stale base could DROP repository files that are not part of this save.
Task 7: review (opus) — spec ✅, quality Approved. ATOMICITY PROVEN from the test, not the claim: commitFiles issues exactly one /git/trees + one /git/commits per commitOnto, so two calls would give two of each and toHaveLength(1) genuinely fails on a split; the single tree is asserted to carry both paths. PUBLISHING UNTOUCHED: `git diff -- src/publish/` is a new file only, and main.ts's diff never enters publishDrawcast. NO DESTRUCTION: deletions:[] plus a base_tree from a readHead taken inside commitFiles, with a 422 re-read-and-rebuild retry — unlisted files survive. Token claim verified independently (setGithubToken writes outside Settings/persist; repo input and token input are the ONLY two mutation paths, both wired).
IMPORTANT: slug collision is silent DATA LOSS — two never-saved docs both titled "Untitled drawcast" mint the same path; the second overwrites the first in HEAD and sourceManifest REPLACES its entry, so the first document's source vanishes from the picker with no warning.
IMPORTANT: destinations.ts and main.ts duplicate the menu labels and are joined by exact-string matching including the ellipsis — rename either side and the item hides forever while both test files stay green.
Ruling: fix both Importants plus two cheap Minors — pin the .yaml as files[0] with a comment and an assertion (in an EMPTY repo commitFiles writes files[0] via the Contents API as its own commit, so the save is two commits and is safe only by accident of array order), and validate ts/title in parseSourceManifest so a hand-edited entry cannot throw on sort. Deferring the persist()-rebuilds-an-open-menu case (unreachable while Settings is modal) and the coursesDir-change double-listing. Cost if wrong: one more review cycle on the task with remote side effects, which is where I most want the extra cycle.
Task 7: fix round 2/5 dispatched. FIX_BASE=b7322c5.
Task 7: fix round 2/5 implemented (commit a25ce83, 160 files / 2654 tests green, tsc clean). uniqueSourcePath suffixes only when a manifest entry belongs to a DIFFERENT document; re-save keeps its path unconditionally. OPEN_LABELS/SAVE_LABELS exported and main.ts reads both the item label and its hidden check from them. .yaml-is-files[0] invariant commented AND pinned by a test proving the empty-repo Contents-API seed call is the .yaml. parseSourceManifest validates title/ts and drops malformed entries.
Task 7: scoped re-review dispatched (sonnet). Told it the way the collision fix could backfire: if a RE-SAVE ever suffixes, every save mints a new file and the repo fills with orphans — so it must trace both branches and name the condition that separates them. Also whether the collision test proves both documents' entries SURVIVE in the committed manifest, since a suffixed path with a replaced manifest entry would still lose document 1.
Task 7: re-review — all FOUR findings ADDRESSED, new breakage none. The re-save branch is distinguished on `args.existing` alone: truthy uses the document's own recorded path and NEVER passes through uniqueSourcePath, so no previously-successful save can have its path changed. The collision test asserts the COMMITTED MANIFEST contains both untitled-drawcast.yaml and untitled-drawcast-2.yaml — the actual requirement, not merely a differing path. Zero duplicated label literals (grep finds one hit in main.ts, a comment). The files[0] test is genuinely order-sensitive: it asserts the empty-repo seed PUT targets the .yaml and not.toContain("index.json"), so swapping the array would fail it. parseSourceManifest drops bad entries individually via filter, with a test proving a sibling still lists.
Task 7: complete (commits 979782f..a25ce83, review clean). PUSHED.
Task 8: dispatched (sonnet — the mark, the favicon, the squiggle's deletion). BASE=a25ce83. LAST TASK.
Task 8: implementer DONE (commit 1ed6d0b, 161 files / 2657 tests green, tsc clean). The mark: a thick hand-drawn stroke sweeping up from lower-left into a solid ink play triangle, fixed seed. I verified the artifact myself before reviewing: public/mark.svg has 3 path elements, ink #3d3833 only, NO accent, viewBox 0 0 64 64, path-point bounds x 5.5-60.0 / y 15.8-52.2 — it fills the box rather than being a speck at favicon size.
Task 8: implementer flagged that src/viewer.ts:115 also carries a `squiggle` class, now inert since the CSS rule is deleted. It left it alone because the brief's file list did not include viewer.ts — correct not to silently expand scope, and worth a decision.
Task 8: review dispatched (sonnet). Told it to check that the COMMITTED mark.svg is genuinely the output of markSvg() — a hand-edited or stale file would make the "drawn by the engine" claim false and future regeneration would silently change the logo — and to assess whether the standalone viewer page now looks wrong without its underline.
Task 8: review — spec ✅, quality Approved. AUTHENTICITY VERIFIED: the reviewer ran `npx vite-node scripts/build-mark.ts` itself and `git status` showed no diff — the committed mark.svg is byte-identical to a fresh regeneration, so "drawn by the engine" is true and a future regeneration will not silently change the logo. Every roughjs call routes through a shared opts() helper that always sets seed — no unseeded drawable exists. Uses generator().toPaths(), never rough.svg(). Favicon replaced and ./mark.svg resolves given vite's base:"./". Squiggle rule and class both gone; accent allowlist untouched and passing. build-mark.ts is outside tsconfig's include and imported by nothing in src.
Task 8: minor (deferred, FOR FINAL TRIAGE): src/viewer.ts:115 still carries an inert `squiggle` class, and its footer still reads "Made with drawcast ✏️" — both outside the brief's file list, both flagged by the implementer rather than hidden. The standalone viewer page now has a bare title, no squiggle and no mark.
Task 8: complete (commits a25ce83..1ed6d0b, review clean). PUSHED.
ALL 8 TASKS COMPLETE. Proceeding to the final whole-branch review (opus).
FINAL WHOLE-BRANCH REVIEW (opus, 15 commits / +1766-302 / 27 files): NOT READY TO MERGE.
CRITICAL — dark mode makes the UI unreadable, and NO test written this round could see it. Task 2 themed --ink; four rules pair a themed foreground with a HARDCODED #fff ground (styles.css:652-659 buttons/selects/inputs, :588-598 textarea, :124-126 .mode-btn.active which Task 1 changed from --rust to --ink, :363-365 .sidebar-new). #ece6da on #fff = 1.24:1. Turn on Appearance→Dark and the spec textarea, every button and select, the mode pill and "New drawcast" go near-invisible — and it reaches published viewer pages, since viewer.ts imports styles.css. The new mark also vanishes: public/mark.svg is hardcoded #3d3833 on dark --paper #1c1a17 = 1.50:1. WHY NOTHING CAUGHT IT: spec §5.3 guarded only the FIGURE against reading chrome tokens, and palette.test.ts asserts token-PAIR ratios — no assertion can see a literal #fff paired with a themed token. Task 1 and Task 2 each did their half correctly; the defect exists only in the combination. This is exactly the cross-cutting class round one's final review found.
IMPORTANT: Save reports success after writing STALE content — ensureRendered returns false on unparseable text, readPlaylistText's red status is then overwritten by "Saving source to GitHub…", and the save proceeds from doc.playlist (the last good render). Break an indent, Save to GitHub, get `Saved "X"`, and the commit holds the previous version.
IMPORTANT: the three Save destinations disagree — disk and GitHub write formatPlaylist(doc.playlist), Drive writes specArea.value. One menu, three answers whenever text differs from render.
Ruling: fix both Importants together — ALL THREE destinations save the editor's TEXT, and a save is REFUSED when that text does not parse, with the parse error left visible rather than overwritten by a progress message. Why: what you see is what you save is the only rule a user can hold, and a false "Saved" is worse than a refusal. Shared refusal path so the three cannot drift apart again, plus a pure tested decision function. Cost if wrong: a save is blocked while the spec is mid-edit and invalid, which is the state where saving was silently useless anyway.
Ruling: the topbar mark becomes INLINE svg using currentColor so it follows --ink; public/mark.svg stays fixed-ink because it is the favicon and favicons cannot theme. Cost if wrong: two representations of one mark, both generated from markSvg().
Deferred triage: data-theme duplication SHIP (must be touched anyway); Task 4's "display formatting" wording SHIP; share.ts:121 "four panels" FIX; viewer.ts inert squiggle + ✏️ footer FIX.
ONE fix wave dispatched (sonnet) with the Critical, both Importants, and items 4-9. FIX_BASE=1ed6d0b.
Final fix wave implemented (commit 70f2bcd, 162 files / 2675 tests green, tsc clean). All 9 items fixed, none skipped. Dark --ink on the old literal #fff was 1.24:1; on the new --field (#302b24) it is 11.29:1. The sweep found SIX white-on-themed sites — two beyond the four the reviewer named. Topbar mark now inline with currentColor (tracks --ink, 11+:1 both themes); public/mark.svg favicon byte-identical. New checkSaveable() in src/ui/save-gate.ts plus a shared prepareSave() make all three Save destinations refuse-and-report instead of shipping stale doc.playlist. A new svgFromMarkup() helper appeared in src/ui/dom.ts using DOMParser.
ONE scoped re-review dispatched (OPUS — last gate). Told it to recompute the contrast for BOTH themes, since a fix that repairs dark while breaking light is a net loss; to run the #fff/#ffffff/white grep ITSELF rather than trust "six sites", checking each survivor is on a genuinely fixed ground like the figure's own paper; to confirm svgFromMarkup's DOMParser is never called at module scope, which would crash the node test environment; and to check the save change did not break the WORKING case, since Drive's filename/mimeType logic depended on format.
Final fix-wave re-review (opus) — ALL NINE ADDRESSED, merge verdict MERGE. Contrast recomputed independently for BOTH themes: dark #ece6da on #302b24 = 11.29:1; light #3d3833 on #fff = 11.59:1 and byte-identical to before, so no light-side loss. .mode-btn.active 9.57:1 light / 13.97:1 dark. --field present in all three blocks. The only surviving literal white is button.primary's #fff on fixed --rust at 5.34:1, correctly left. prepareSave ignores ensureRendered's return, re-derives from specArea.value, sets the red status and returns null; no caller posts "Saving…" after it. All three destinations ship save.text; Drive's filename logic intact because save.title uses the SAME docTitleOf ensureRendered uses. mark.svg byte-unchanged and markSvg(64) still reproduces it exactly (reviewer ran it). svgFromMarkup is only called at main.ts module scope, which no test imports.
RESIDUAL 1 (the ruled sweep was incomplete): the sweep matched literal #fff/white but not NEAR-white literals. .cs-cardgate-q puts var(--ink) on .cs-cardgate-card's rgba(255,253,246,0.93) over the deliberately unthemed figure stage — ~1.23:1 in dark. THE FLASHCARD QUESTION IS INVISIBLE IN DARK MODE, on published viewer pages too. Same pattern at .cs-bigplay:hover and .cs-keyguide-key. The new palette assertion's regex cannot see it.
RESIDUAL 2 (new Minor from the fix wave): a refused disk save returns before saveDiskModal.dialog.close(), so the modal and its backdrop cover .editor-status — the button reads as dead rather than showing why it refused.
Ruling: DEPART from the one-fix-wave rule and dispatch a second, tightly-scoped fix. Why: the skill's single-wave rule exists to stop endless cycles, and I am not opening a cycle — these are two confirmed, user-visible defects, both inside the scope I ruled, and one of them (an invisible quiz question) is a regression THIS ROUND created: those CSS rules predate it, but their broken effect exists only because this round added dark mode. Shipping it and mentioning it in a summary would be worse than one more twenty-minute pass. Cost if wrong: one extra review cycle at the end of a round that is otherwise finished.
Residual fix implemented (commit cb4b12c, 162 files / 2680 tests green, tsc clean) and re-reviewed: BOTH ADDRESSED, no new breakage, ROUND CLOSES CLEAN. All four sites carry fixed literals; the fixer found a FOURTH beyond my three (the card's own border). figure-style.ts verified untouched by git diff. isNearWhite genuinely parses channels (>=240/235/225) rather than matching text, and a cross-rule parent/child test catches the .cs-cardgate-card/.cs-cardgate-q split that ANY same-rule scan is structurally blind to. No survivors in the sweep. LIGHT MODE UNCHANGED: #3d3833 and #6f685c are byte-identical to the light --ink/--muted, so the fix is invisible in light and decisive in dark. Disk modal closes unconditionally before the guard; prepareSave is pure so the success path is untouched and a double close is a no-op.
Parked: isNearWhite has no hsl() branch and its rgba regex requires commas, so CSS Color-4 space syntax (`rgb(255 253 246)`) would slip past. Ruling: park it — grepped, zero occurrences of either form exist in styles.css today, so the gap is latent rather than exploited, and widening the parser speculatively is how a guard becomes unmaintainable. Cost if wrong: a future near-white written in a form the parser misses reaches dark mode unchecked.
ROUND TWO COMPLETE. 17 commits, all pushed.
